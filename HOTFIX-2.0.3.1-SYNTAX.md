# 🔥 HOTFIX v2.0.3.1: Исправление синтаксической ошибки

## ❌ Проблема

После обновления до v2.0.3 возникла **критическая ошибка**:

```
Unexpected token, expected ","
```

**Причина:**
При автоматическом добавлении параметра `dragBoundFunc` во все виджеты был пропущен символ **запятая** после `onTransformEnd`.

**Затронутые файлы:**
- ❌ ButtonWidget.tsx
- ❌ ImageWidget.tsx
- ❌ MenuWidget.tsx
- ❌ TextWidget.tsx
- ❌ VideoWidget.tsx

---

## ✅ Решение

Добавлена запятая после `onTransformEnd` во всех виджетах.

### До (неправильно):
```typescript
const MenuWidget: React.FC<MenuWidgetProps> = ({
  widget,
  isSelected,
  onSelect,
  onDragEnd,
  onTransformEnd     ← Здесь нет запятой!
  dragBoundFunc
}) => {
```

### После (правильно):
```typescript
const MenuWidget: React.FC<MenuWidgetProps> = ({
  widget,
  isSelected,
  onSelect,
  onDragEnd,
  onTransformEnd,    ← Запятая добавлена
  dragBoundFunc
}) => {
```

---

## 🔧 Исправленные файлы

✅ ButtonWidget.tsx  
✅ ImageWidget.tsx  
✅ MenuWidget.tsx  
✅ TextWidget.tsx  
✅ VideoWidget.tsx  

ShapeWidget.tsx не требовал исправления - там изначально было правильно.

---

## 📦 Обновление

**Скачайте исправленный архив:**
- kiosk-content-platform.zip (v2.0.3.1)

**Или исправьте вручную:**

Откройте каждый файл и добавьте запятую после `onTransformEnd`:

```bash
# Найдите строку
onTransformEnd

# Замените на
onTransformEnd,
```

---

## ✅ Проверка

После обновления:

```bash
npm run dev:editor
```

Должно запуститься без ошибок ✅

---

## 🙏 Извинения

Приносим извинения за неудобства. Это была ошибка в автоматическом скрипте обновления.

**Версия:** 2.0.3.1 (hotfix)  
**Дата:** Декабрь 2025  
**Статус:** ✅ Исправлено
