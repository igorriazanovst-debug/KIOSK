# Журнал изменений (CHANGELOG-DEV)

> Лог багов, исправлений, новых фич. Заполняется по результату каждого изменения в проекте.
> Формат записи: дата, тип (BUG/FIX/FEATURE/REFACTOR/CHORE), краткое описание, причина, способ исправления, затронутые файлы.

---

## 2026-09-09 (продолжение 9)

### FEATURE — Этап 2b, волна 9 (ТРЕТЬЯ УКРУПНЁННАЯ) — 1709 заданий, 84 темы
- **Что сделано:** тот же темп, что волны 7-8 — 14 тем/388 заданий за один заход. Продолжение прогрессий: «Вычитание: числа до 200» (+30), «Сложение: числа до 200» (+30), «Состав числа: числа 71-100» (+60, впервые 3 группы вместо 2 — диапазон из 30 значений), «Сравнение: числа до 500» (+24), «Порядок чисел: семь чисел» (+20, впервые 7-элементные серии), «Порядковые числительные: ряды по 15-16» (+24), «Умножение: на 13 и 14» (+20), «Деление: делители 21-25» (+24), «Кратные: 26-30» (+30), «Доли целого: седьмая и девятая части» (+24), «Пропущенное число: шаг 8 и 9» (+24), «Счёт: до 80» (+24), «Сумма трёх чисел: числа до 80» (+24), «Оценки: ещё примеры округления до тысяч» (+30, тот же диапазон 1000-9999 волны 8, новые числа — проверено тестом на отсутствие пересечения с волной 8). Итого +388 заданий, +14 тем (70→84), +33 группы (134→167). Новых `TaskTypeId` нет.
- 4 из 14 тем — choice-режима (Сравнение, Порядок чисел, Деление, Кратные) и потребовали брутфорс-поиска соли каждая (0 из 4 прошли с первой выбранной вручную солью — устойчивый паттерн во всех укрупнённых волнах).
- **Файлы:** `packages/player/src/mathmachine/content/{generateWave9Content.ts,generateWave9Content.test.ts,runGenerateWave9.ts,pilotContent.json,pilotContent.test.ts}`.
- **Живая проверка через собранный `.exe` (CDP):** все 14 новых тем волны прогнаны полностью — 11/14 «Верно!» напрямую; 2 ложных «НЕ УСПЕХ» («Порядок чисел: семь чисел», «Кратные: 26-30») — тот же класс ограничения тестового скрипта (общий текст вопроса между заданиями/волнами), оба вручную подтверждены по `pilotContent.json` как реально верные; «Счёт: до 80» отдельно перепроверено точечным CDP-скриптом (61 точка, ответ принят). Подсказка/раскрытие и все 3 инструмента лаборатории отработали штатно. 0 ошибок консоли.
- **Проверка:** player 341/341 (включая полную статистическую батарею на весь каталог из 167 групп), `tsc --noEmit` чист. Коммит `f8c3946`.

---

## 2026-09-09 (продолжение 8)

### FEATURE — Этап 2b, волна 8 (ВТОРАЯ УКРУПНЁННАЯ) — 1321 задание, 70 тем
- **Что сделано:** тот же темп, что и волна 7 — 14 тем/368 заданий за один заход. Продолжение прогрессий: «Вычитание: числа до 100» (+30), «Сложение: числа до 100» (+30), «Состав числа: числа 51-70» (+40), «Сравнение: числа до 200» (+24), «Порядок чисел: шесть чисел» (+20, впервые 6-элементные серии), «Порядковые числительные: ряды по 13-14» (+24), «Деление: делители 16-20» (+24), «Кратные: 21-25» (+30), «Доли целого: шестая и восьмая части» (+24), «Пропущенное число: шаг 6 и 7» (+24), «Счёт: до 60» (+24), «Сумма трёх чисел: числа до 60» (+24) — плюс два принципиально новых расширения по решению пользователя: «Умножение: на 11 и 12» (+20, впервые с волны 2 расширен множитель) и «Оценки: округление до тысяч» (+30, впервые числа 1000-9999 — новый порядок величины для аудитории 4-9 лет; волна 4 сознательно останавливалась на сотнях именно по этой причине, пользователь подтвердил включение). Итого +368 заданий, +14 тем (56→70), +32 группы (102→134). Новых `TaskTypeId` нет.
- 4 из 14 тем — choice-режима (Сравнение, Порядок чисел, Деление, Кратные) и потребовали брутфорс-поиска соли каждая (0 из 4 прошли с первой выбранной вручную солью).
- **Файлы:** `packages/player/src/mathmachine/content/{generateWave8Content.ts,generateWave8Content.test.ts,runGenerateWave8.ts,pilotContent.json,pilotContent.test.ts}`.
- **Живая проверка через собранный `.exe` (CDP):** все 14 новых тем волны прогнаны полностью — 11/14 «Верно!» напрямую; 2 ложных «НЕ УСПЕХ» («Порядок чисел: шесть чисел», «Кратные: 21-25») — тот же класс ограничения тестового скрипта (общий текст вопроса между заданиями/волнами), оба вручную подтверждены по `pilotContent.json` как реально верные; «Счёт: до 60» отдельно перепроверено точечным CDP-скриптом (41 точка, ответ принят). Подсказка/раскрытие и все 3 инструмента лаборатории отработали штатно. 0 ошибок консоли.
- **Проверка:** player 328/328 (включая полную статистическую батарею на весь каталог из 134 групп), `tsc --noEmit` чист. Коммит `756c973`.

---

## 2026-09-09 (продолжение 7)

### FEATURE — Этап 2b, волна 7 (УКРУПНЁННАЯ) — 953 задания, 56 тем
- **Что сделано:** по решению пользователя (при текущем темпе ~100 заданий/волну до цели ТЗ FR-020 в 2800+ понадобилось бы ~20+ волн) — волна из 13 тем/348 заданий за один заход вместо обычных ~6/~100. Расширение числового охвата уже существующих 13 категорий, без новых тем ради темы (FR-021, 40+ тем, уже перекрыт волной 6): «Вычитание: числа до 50» (+30), «Сложение: числа до 50» (+30), «Состав числа: числа 31-50» (+40, впервые набор разложений шире краёв — добавлена середина `floor(whole/2)`), «Сравнение: числа до 100» (+24), «Порядок чисел: пять чисел» (+20, впервые 5-элементные серии), «Порядковые числительные: ряды по 11-12» (+24), «Кратные: 16-20» (+30), «Деление: делители 13, 14, 15» (+24), «Доли целого: пятая и десятая части» (+24, впервые parts=5,10), «Пропущенное число: шаг 4 и 5» (+24), «Счёт: до 40» (+24), «Сумма трёх чисел: числа до 40» (+24), «Оценки: больше примеров округления до сотен» (+30, те же 100-999, новые числа, ни одно не пересекается с волной 4). Итого +348 заданий, +13 тем (43→56), +30 групп (72→102). Новых `TaskTypeId` нет.
- 4 из 13 тем — choice-режима (Сравнение, Порядок чисел, Кратные, Деление) и защищены батареей инвариантов; соли для всех четырёх подобраны брутфорс-поиском (первые выбранные вручную не прошли батарею ни разу — при бОльшем масштабе волны количество choice-групп, требующих подбора соли, тоже выросло пропорционально).
- **Файлы:** `packages/player/src/mathmachine/content/{generateWave7Content.ts,generateWave7Content.test.ts,runGenerateWave7.ts,pilotContent.json,pilotContent.test.ts}`.
- **Живая проверка через собранный `.exe` (CDP):** прогон впервые охватил ВСЕ 13 новых тем волны (не выборку из 6, как в волнах 4-6) — 11/13 «Верно!» напрямую; 2 ложных «НЕ УСПЕХ» («Порядок чисел: пять чисел», «Кратные: 16-20») — тот же класс ограничения тестового скрипта, что и в предыдущих волнах (общий текст вопроса между несколькими заданиями/волнами, плоский `text→answer` lookup); оба вручную подтверждены по `pilotContent.json` как реально верные. «Счёт: до 40» отдельно перепроверено точечным CDP-скриптом (подсчёт точек по `border-radius:50%`, ввод точного числа) — «Верно!», подтверждает масштабирование визуала до 21 точки без правок кода. Подсказка/раскрытие и все 3 инструмента лаборатории отработали штатно. 0 ошибок консоли.
- **Проверка:** player 315/315 (включая полную статистическую батарею на весь каталог из 102 групп), `tsc --noEmit` чист. Коммит `3009541`.

---

## 2026-09-09 (продолжение 6)

### FEATURE — Этап 2b, волна 6: количественное расширение (605 заданий, 43 темы)
- **Что сделано:** 6 новых тем, продолжающих прогрессии, начатые предыдущими волнами: «Сложение трёх чисел: числа до 20» (+16, впервые с Этапа 1 расширен диапазон сумм ≤7), «Кратные: 11-15» (+20, продолжение волны 5's 6-10), «Деление: делители 11 и 12» (+16, продолжение волны 3's 2-10), «Порядковые числительные: ряд из 10 чисел» (+16, продолжение волны 5's 8-9; серии впервые строятся формулой `base + (j*step) mod 10` для step, взаимно простого с 10, вместо ручного набора массивов — исключает риск опечатки с повторяющимся числом внутри серии), «Доли целого: половина и четверть больших чисел» (+16, тоталы 40-68 вместо 4-36), «Состав числа: числа 21-30» (+20, продолжение волны 5's 11-20). Итого +104 задания, +6 тем (37→43), +12 групп (60→72). Новых `TaskTypeId` нет.
- Только 2 из 6 тем — choice-режима («Кратные», «Деление») и защищены батареей инвариантов; соли подобраны брутфорс-поиском (`w6-mult-d1-0`/`w6-mult-d2-5`/`w6-mult-position-1` и `w6-div-scheme-0`/`w6-div-position-8`) — первые выбранные вручную не прошли батарею, как и во всех предыдущих волнах.
- **Файлы:** `packages/player/src/mathmachine/content/{generateWave6Content.ts,generateWave6Content.test.ts,runGenerateWave6.ts,pilotContent.json,pilotContent.test.ts}`.
- **Живая проверка через собранный `.exe` (CDP):** все 6 новых тем работают корректно — 5/6 «Верно!» напрямую в автоматическом прогоне; 1 ложное «НЕ УСПЕХ» («Кратные: 11-15») — тот же класс ограничения тестового скрипта, что и в волнах 4-5 (текст вопроса общий для всех 4 заданий группы `kratn3_1112`, плоский `text→answer` lookup взял ответ от чужого задания); вручную подтверждено в `pilotContent.json` — реально показанные варианты были `19, 22, 20`, `correctAnswer=22` — виджет работает верно. Подсказка/раскрытие и все 3 инструмента лаборатории отработали штатно. 0 ошибок консоли.
- **Проверка:** player 302/302 (включая полную статистическую батарею `epicTwelveStatisticalAudit.test.ts`, которая динамически покрывает все choice-группы каталога — новые группы волны 6 прошли её без отдельной донастройки), `tsc --noEmit` чист. Коммит `b75272b`.

---

## 2026-09-09 (продолжение 5)

### FEATURE — Этап 2b, волна 5: количественное расширение, балансировка каталога (501 задание, 37 тем)
- **Что сделано:** вместо дальнейшего наращивания уже насыщенных категорий (Умножение — 8 тем/90 заданий) — 6 новых тем в 5 категориях, у которых на момент планирования была ровно 1 тема: «Сравнение: числа до 50» (+16, диапазон 1-10→15-46), «Состав числа: числа 11-20» (+20), «Порядок чисел: четыре числа» (+16, впервые 4-элементные серии вместо 3), «Счёт: до 20» (+16, впервые >7 предметов — визуал `TaskVisual` с `flexWrap` масштабируется без правок кода), «Пропущенное число: длинные ряды» (+16, шаг 2/3 вместо шага 1), плюс «Порядковые числительные: ряды по 8-9» (+16, углубление волны 4). Итого +100 заданий, +6 тем (31→37), +12 групп (48→60). Новых `TaskTypeId` нет.
- Только 2 из 6 тем — choice-режима (Сравнение, Порядок чисел) и защищены той же батареей инвариантов (частота/период/линейная формула), что и предыдущие волны; соль позиции кнопки для «Сравнения» подобрана брутфорс-поиском (`w5-cmp-position-150`) — первая соль не прошла батарею. Остальные 4 темы — numeric-режим, экплойт через позицию кнопки для них структурно невозможен.
- **Файлы:** `packages/player/src/mathmachine/content/{generateWave5Content.ts,generateWave5Content.test.ts,runGenerateWave5.ts,pilotContent.json,pilotContent.test.ts}`.
- **Живая проверка через собранный `.exe` (CDP):** все 6 новых тем открываются и работают корректно. Автоматический прогон дал 4/6 «Верно!» напрямую; 2 ложных «НЕ УСПЕХ» — оба тот же класс ограничения тестового скрипта, что уже документирован для волны 4: (1) «Порядок чисел: четыре числа» — текст вопроса общий для всех заданий групп `order_min`(волна 1)/`order4_min`(волна 5), плоский `text→answer` словарь взял ответ от чужого задания; вручную подтверждено — реально показанные варианты были `8,11,1,4`, виджет корректно принял `1`; (2) «Счёт: до 20» — `getTaskText()` берёт ПОСЛЕДНИЙ `<span>` на странице, а для `number_counting` последними идут спаны-точки визуала, не текст задания (та же причина, по которой тема «Счёт» Этапа 1 никогда не проверялась этим скриптом); отдельной точечной проверкой (подсчёт спанов по `border-radius:50%`, ввод правильного числа точек) подтверждено «Верно!». Подсказка/раскрытие («Сложение») и все 3 инструмента лаборатории отработали штатно. 0 ошибок консоли.
- **Проверка:** player 292/292 (все тесты, включая 9 новых wave5-тестов), `tsc --noEmit` чист. Коммит `cbbbaed`.

---

## 2026-09-09 (продолжение 4)

### FEATURE — Этап 2b, волна 4: количественное расширение контента (401 задание, 31 тема)
- **Что сделано:** углублены 6 уже покрытых категорий новыми темами (не новыми типами заданий — все 13 категорий ТЗ FR-022 закрыты волнами 1-3): «Вычитание: переход через десяток» (+16), «Сложение: числа до 20» (+16), «Кратные: 6-10» (+20), «Порядковые числительные: длинные ряды» (+16), «Оценки: округление до сотен» (+16), «Доли целого: треть» (+8). Итого +92 задания, +6 тем (25→31), +12 групп (37→48). `schema.ts`/`taskEngine.ts`/`TaskVisual.tsx`/`TaskRunner.tsx` не тронуты — новых `TaskTypeId` нет.
- **Соли для позиции/дистракторов** (`w4-mult-d1-0`/`w4-mult-d2-8`/`w4-mult-position-9` и др.) подобраны брутфорс-поиском (`salt_search_wave4.mjs`) против той же батареи инвариантов, что и в предыдущих волнах (частота/период/линейная формула/медиана) — первые выбранные вручную соли не прошли батарею с первого раза, как и в волнах 1/2.
- **Файлы:** `packages/player/src/mathmachine/content/{generateWave4Content.ts,generateWave4Content.test.ts,runGenerateWave4.ts,pilotContent.json,pilotContent.test.ts}`.
- **Живая проверка через собранный `.exe` (CDP):** все 6 новых тем открываются и принимают верный ответ («Верно!» показано для 5/6 в автоматическом прогоне; 6-я — «Кратные: 6-10» — ложное «НЕ УСПЕХ» из-за коллизии тестового скрипта: несколько заданий группы `kratn2_67` делят один и тот же текст задания на разные наборы вариантов/ответов, а тестовый `task_lookup.json` — плоский словарь `text→answer`, взял не тот ответ; вручную подтверждено в `pilotContent.json`, что реально показанные варианты 9/11/12 и `correctAnswer=12` совпадают — виджет работает верно, дефект не в продакшен-коде, а в упрощённом test-lookup). Подсказка/раскрытие и все 3 инструмента лаборатории (Весы/Цепочка/Два отрезка) отработали штатно, 0 ошибок консоли.
- **Проверка:** player 401/401 задание в схеме валидны, `tsc --noEmit` чист. Коммит `69c71f5`.

---

## 2026-09-09 (продолжение 3)

### FEATURE — статистически откалиброванная проверка Эпика 12
- **Что было:** тесты волн проверяли детерминированные пороги (частота позиции ≤ ceil(G/2), эвристика решает ≤ половины и т.п.) — это устраняет очевидные эксплойты, но не то же, что оригинальная методология («нулевые модели по 500k случайных конструкций», см. Тип6_бэклог.md, Эпик 12).
- **Способ:** для каждой оси (позиция кнопки во всех choice-группах; структурные эвристики деления majority/odd-one-out; медиана в «Цифрах»/«Кратных»; порядок называния чисел в тексте «Сравнения») строится распределение метрики эксплуатируемости на 20000 случайных конструкций ТОГО ЖЕ размера (детерминированный PRNG mulberry32, воспроизводимо), наблюдаемый хэш-based результат сравнивается с этим распределением. Критерий: ≥5% случайных конструкций дают равную или большую эксплуатируемость — иначе конструкция статистически значимо предсказуемее шума, даже если наивный порог формально пройден.
- **Файлы:** `packages/player/src/mathmachine/content/epicTwelveStatisticalAudit.test.ts`.
- **Результат:** глубоко в безопасной зоне на всех осях — позиция кнопки безопаснее 96.5% случайных вариантов (min percentile 0.965), структурные эвристики деления — 33.8%, медиана — 78.3%, порядок слов в тексте «Сравнения» — 100% (никогда не хуже случайного). player 274/274 (+4 новых теста), `tsc --noEmit` чист. Коммит `b72bbe9`.

---

## 2026-09-09 (продолжение 2)

### REFACTOR — общий модуль для дублированных хелперов генераторов контента
- **Что было:** `buildGroup`/`rotate`/`contentHash`(+`fnv1a`/`avalanche`)/`DIVISION_DECOY_SCHEMES`/`formatDivision`/`divideTask`/`mergeIntoContent` дублировались по себе в `generateWave1/2/3Content.ts` — осознанное решение спеки волны 2 («каждый скрипт волны самодостаточен»), но при переделке Эпика 12 правки приходилось вносить в 2-3 местах одновременно.
- **Способ:** вынесены в `generatorShared.ts` — только чистая инфраструктура; сама волна (задания/темы/диапазоны) осталась в каждом `generateWaveNContent.ts`. Соли для `divideTask` передаются явно на месте вызова (без изменений значений), не берутся из общего дефолта.
- **Файлы:** `packages/player/src/mathmachine/content/{generatorShared.ts,generatorShared.test.ts,generateWave1Content.ts,generateWave2Content.ts,generateWave3Content.ts}`.
- **Проверка:** чисто структурный рефакторинг — `pilotContent.json` перегенерирован от базы Этапа 1 через все три `runGenerateWaveN.ts` и совпал ПОБАЙТОВО (`diff` exit 0) с состоянием до рефакторинга. player 270/270 (было 259 + 11 новых тестов), `tsc --noEmit` чист. Коммит `5d17bc1`.

---

## 2026-09-09 (продолжение)

### FEATURE — офлайн-озвучка 18 пилотных заданий Этапа 1
- **Что сделано:** реализован Task 16 исходного плана (Windows System.Speech SAPI + ffmpeg → mp3) — оригинальные аудиофайлы восстановить было нельзя (бинарные, не в JSONL-транскрипте), сгенерированы заново тем же механизмом.
- **Найдено по пути:** (1) `./media/${id}.mp3` в `TaskRunner.tsx` резолвится относительно `dist/index.html` — файлы должны лежать в `packages/player/public/media/` (vite копирует `public/*` в `dist/*`), а не в `src/mathmachine/media/`, откуда vite их не подхватит для production-сборки (динамический путь недоступен статическому анализу Rollup); (2) служба Windows Audio (`AudioSrv`) была зависшей на машине — SAPI тихо отдавал 46-байтовые пустые WAV без единой ошибки, независимо от текста; `Restart-Service AudioSrv` устранило полностью, добавлена защита (порог размера WAV + до 3 повторных попыток) на случай повтора.
- **Способ исправления:** `generate_pilot_audio.ps1` (только TTS+ffmpeg, не трогает JSON) + `fill_pilot_audio_ids.mjs` (точечно проставляет `audioTaskTextId`, безопасным `JSON.parse`/`stringify`, не PowerShell `ConvertTo-Json`, которая переформатировала бы весь 309-задачный файл).
- **Файлы:** `packages/player/public/media/*.mp3` (18 файлов), `packages/player/src/mathmachine/content/{generate_pilot_audio.ps1,fill_pilot_audio_ids.mjs,pilotContent.json,pilotContent.test.ts}`.
- **Проверка:** живьём через packaged Electron — `Audio()` успешно загружает файл изнутри упакованного `.exe` (asar-прозрачность), длительность точно совпадает с ffprobe. Новый тест: каждое задание с `audioTaskTextId` имеет существующий mp3 на диске. player 259/259, `tsc --noEmit` чист. Коммит `a40df66`.

---

## 2026-09-09

### CHORE — восстановление ветки `feat/mathmachine-widget` после потери локального клона
- **Симптом:** локальный git-клон (единственный, ветка не была запушена) удалён случайно командой очистки `%TEMP%`. Потеряна вся история коммитов Этапа 1, Этапа 2a, Этапа 2b волн 1-3 и часть кросс-волнового фикса позиционной предсказуемости (Эпик 12).
- **Причина:** ветка велась в scratchpad-директории задачи, не в постоянном месте, и ни разу не пушилась на GitHub. Новое правило: теперь всё коммитится и пушится на каждом значимом шаге, ветка ведётся в постоянном клоне.
- **Способ восстановления:** новый клон `C:\recovery_work\kiosk-repo`, ветка `feat/mathmachine-widget` пересоздана поверх актуального `main` (форк-пойнт `db771d1`). Источник — JSONL-транскрипт сессии (планы фичи содержат полный рабочий код по конвенции проекта «No Placeholders») + отчёты диспетчеризованных субагентов (`<task-notification>`) для деталей фикс-раундов, которые не попадают в JSONL напрямую. `STATUS.md`/`Тип6_бэклог.md` (вне git, `C:\Users\Алексей\Desktop\kiosk admin\`) пережили удаление и дали точное повествовательное описание финального состояния — включая коммит-хэши и точные значения фикса R1.
- **Результат:** Этап 1 (18 заданий) + Этап 2a (Весы/Цепочка/Два отрезка) + Этап 2b волны 1-3 (309 заданий/25 тем/37 групп) + фикс R1 (`grp_div_67`) переисполнены и запушены заново, каждый шаг — отдельным коммитом с прошедшими тестами (`packages/shared` 327/327, `packages/player` 68/68 из mathmachine-relevant наборов) и чистым `tsc --noEmit`.
- **Известные дефекты, воспроизведённые намеренно (не переделывались — см. «добавлено в доработку» ниже), т.к. точное восстановление 11-раундового статистически откалиброванного фикса потребовало бы отдельной многочасовой работы:**
  1. **Волна 1** — позиция правильного варианта в choice-группах (Сравнение, Цифры, Упорядочение) константна внутри каждой группы — решается без чтения условия.
  2. **«Сравнение» (волна 1)** — текст вопроса всегда называет числа по возрастанию («Какое число больше: {a} или {b}?» при `a<b`) — 100%-решаемость по свойству текста, не позиции кнопки. Самая серьёзная из известных находок оригинальной ветки.
  3. **«Кратные»/«Деление» (волна 2)** и **«Деление» делители 6-10 (волна 3, кроме уже исправленного R1)** — не защищены полной батареей структурных эвристик (медиана/majority/odd-one-out и т.д.), которую в оригинале добавил Эпик 12.
  4. **F2** — «остаток» и «частное» в вариантах деления решались по отдельности, без полного деления (оригинал чинил это раундами 10-11 после Эпика 12).
  5. **18 файлов озвучки** (Этап 1, Задача 16) — бинарные, невосстановимы из текстового транскрипта; `audioTaskTextId` оставлены `null`/не заданы, как до Задачи 16.
- **Добавлено в доработку:** переделка Эпика 12 (единая батарея тестов-инвариантов на периодичность позиции + числовой ранг + структурные эвристики, хэш-based ротация вместо `positionIndex`, новые соли — не обязательно совпадающие с оригинальными) — отдельная задача, приоритет и объём подтверждает пользователь перед стартом. Полная техническая расшифровка каждого оригинального раунда — `Тип6_Матемашка\Тип6_бэклог.md`, разделы «Эпик 12» и «Не сделано / открыто по Этапу 2b волна 3» (вне этого репозитория).
- **Файлы:** вся ветка `feat/mathmachine-widget` (см. коммиты `b5ef700`..`740fab8`).

### FIX — после подсказки повторная попытка была недоступна (найдено живой проверкой)
- **Симптом:** после первой неверной попытки текст говорит «Попробуй ещё раз. Подсказка: ...», но поле ввода/кнопки-варианты заблокированы, а повторный клик «Ответить» ничего не делает — единственный выход был полный рестарт задания (сбрасывающий счётчик попыток), из-за чего фаза «раскрыть ответ» (после 2 попыток) была недостижима через обычную игру.
- **Причина:** `AnswerInput`'s `disabled={phase !== 'answering'}` и guard внутри `handleSubmit` (`phase !== 'answering'`) блокировали ЛЮБУЮ фазу кроме исходной, включая `'hint'`, для которой блокировка была явно неверна по смыслу текста подсказки.
- **Способ исправления:** оба условия заменены на явную проверку терминальных фаз — `phase === 'success' || phase === 'revealed'`. Дефект унаследован из Этапа 1, найден живой проверкой на пересобранном `.exe` через CDP (не внесён при реставрации ветки).
- **Файлы:** `packages/player/src/mathmachine/TaskRunner.tsx`.
- **Проверка:** живьём через packaged Electron + CDP — 1-я ошибка → подсказка с разблокированным вводом; 2-я ошибка → корректный переход в `'revealed'` с показом правильного ответа. 0 ошибок консоли. Юнит-тесты 252/252, `tsc --noEmit` чист. Коммит `921a475`.

### FIX — переделка Эпика 12: позиционная и структурная предсказуемость во всех трёх волнах контента
- **Симптом:** в choice-режимных заданиях (варианты-кнопки) правильный ответ можно было угадать без арифметики — по позиции кнопки (цикл, зависящий от номера задания, не от содержания), по тексту вопроса («Сравнение» всегда называло числа по возрастанию — 100%-решаемость), по форме дистракторов (медиана трёх чисел в «Цифрах»/«Кратных» всегда была верным ответом), либо комбинацией «большинство по частному» + «большинство по остатку» в «Делении» волны 2 (16/16 без единого деления).
- **Причина:** ротация вариантов использовала `positionIndex` (номер задания в группе) вместо содержания задания; дистракторы «Цифр»/«Кратных» всегда были `target±1`; текст «Сравнения» называл числа в фиксированном порядке `a, b`; дистракторы деления волны 2 держали тот же частное ИЛИ тот же остаток, что верный ответ.
- **Способ исправления:** ротация позиции и выбор схемы дистракторов теперь выводятся из хэша содержания задания (FNV-1a + avalanche-перемешивание, разные соли на разные оси — позиция кнопки, порядок слов в тексте, выбор дистрактора — независимы друг от друга). Дистракторы «Цифр»/«Кратных» выбираются из более широкого пула смещений. Схема дистракторов «Деления» волны 2 заменена на 4 циклических варианта (та же, что уже проверена в волне 3), пары `7:5`/`8:5` (частное=1, несовместимо с новой схемой) заменены на `11:5`/`14:5`. Волна 3 (уже содержавшая точечный R1-фикс) переведена с `positionIndex` на тот же хэш-механизм — все три её группы деления раньше давали байт-в-байт идентичную последовательность позиций.
- **Файлы:** `packages/player/src/mathmachine/content/generateWave{1,2,3}Content.ts` (+`.test.ts`), `pilotContent.json` (регенерирован).
- **Проверка:** новая батарея тестов (частота позиции / периодичность / линейная формула `a·i+c mod N` / majority-по-частному+остатку / odd-one-out / q−r=const / медиана / порядок слов в тексте) — 0 нарушений по всем choice-группам каталога (подтверждено отдельным сквозным скриптовым прогоном сверх юнит-тестов). player 258/258, `tsc --noEmit` чист. Контент регенерирован от базы Этапа 1 — побайтовое сравнение подтвердило, что изменились только затронутые choice-задания (87 из 309), остальные 222 не тронуты. Коммиты `595da64` (волна 1), `c276be1` (волна 2), `965ef71` (волна 3).
- **Не входило в объём:** статистическая калибровка на случайном шуме (500k+ конструкций) — заменена детерминированной проверкой, достаточной для этого размера групп (G=8-10), но не тем же уровнем формального доказательства, что в оригинале.

---

## 2026-07-08

### FIX — имя установленного приложения (ярлык) не совпадало с «Имя приложения» из редактора
- **Симптом:** при установке сгенерированного .exe ярлык на рабочем столе / в меню «Пуск» назывался `Kiosk Player` вместо имени, заданного в поле «Имя приложения» диалога сборки.
- **Причина:** `packages/player/package.json` → `build.nsis.shortcutName` жёстко зашит как `"Kiosk Player"`. `builds.js` при сборке подставлял `appName` только в `build.productName` (папка установки + запись в «Установка/удаление»), но не в `nsis.shortcutName` (видимое имя ярлыка).
- **Способ исправления:** в `buildDistribution` (`packages/server/src/routes/builds.js`) после установки `productName` дополнительно `packageJson.build.nsis.shortcutName = appName`. Маркер: `SHORTCUT-NAME-FROM-APPNAME`. Цепочка: `BuildDialog.tsx` (поле «Имя приложения» → `appName`) → POST `/api/builds` → `buildDistribution`.
- **Файлы:** `packages/server/src/routes/builds.js` (+ на сервере пропагируется в `dist/routes/builds.js` при `npm run build` сервера).
- **Проверка:** пересобрать сервер, собрать .exe через диалог с непустым именем → установщик создаёт ярлык с этим именем.


### REVERT — откат прогресс-оверлея «тяжёлого проекта» при загрузке
- **Симптом:** оверлей-предупреждение об открытии «тяжёлого» проекта (>250 КБ HTML в browser-menu pages) с окном 2.5 сек на «Прервать» работал плохо — мешал открытию проектов.
- **Что откачено (из коммита `e6d74de8`):**
  - Удалён компонент `packages/editor-web/src/components/ProjectLoadingOverlay.tsx`.
  - `Editor.tsx` — убраны импорт `ProjectLoadingOverlay` и его рендер `<ProjectLoadingOverlay />` (восстановлена версия `e6d74de8~1`).
  - `editorStore.ts` — возвращён простой `loadProject`; убраны поля `loadingStage`/`loadingProgress`/`loadAbortController` и методы `cancelLoadProject`/`dismissLoading` (из interface и implementation) — восстановлена версия `e6d74de8~1`.
- **Оставлено из того же коммита (НЕ откатывалось):** серверный `/api/import` (`packages/server/src/routes/import.js`, регистрация в `app.ts`), nginx-правило, кнопка «Импорт» в `Toolbar.tsx` (`IMPORT-JSON-BTN`).
- **Способ:** `git checkout e6d74de8~1 -- Editor.tsx editorStore.ts` + `git rm ProjectLoadingOverlay.tsx`. Полная обратимость: исходное состояние сохранено меткой `pre-revert-heavy-overlay-backup` (→ `e6d74de8`).
- **Файлы:** `packages/editor-web/src/components/Editor.tsx`, `packages/editor-web/src/components/ProjectLoadingOverlay.tsx` (удалён), `packages/editor-web/src/stores/editorStore.ts`.
- **Проверка:** `git grep` не находит следов оверлея в изменённых файлах; кнопка «Импорт» и серверный импорт на месте. Требуется rebuild+deploy editor-web (`npm run build` → `cp -r dist/* /opt/kiosk/editor-web/`).

---

## 2026-06-03

### FIX — копировался не тот .exe при сборке плеера
- **Симптом:** на устройстве в плеере старый контент, хотя в БД свежая версия проекта.
- **Причина:** в `buildDistribution` (`packages/server/src/routes/builds.js`, шаг 7) выбор .exe через `files.find(f => f.endsWith('.exe') && f.includes('Setup'))` брал ПЕРВЫЙ попавшийся .exe по порядку readdir. В `dist-electron/` накопились старые .exe от прошлых тестовых сборок (`BadLuck-*`, `OldHorse`, `Tuna`, `button-go-*`) → копировался чужой старый установщик.
- **Способ исправления (v2):**
  1. Перед запуском `electron:build:win` удаляются все старые `*Setup*.exe` из `dist-electron/`.
  2. После сборки .exe выбирается новейший по `mtime` (через `fs.stat` + `reduce`).
  - Маркеры идемпотентности: `FIX-EXE-SELECTION-V2`.
- **Файлы:** `packages/server/src/routes/builds.js`, `packages/server/dist/routes/builds.js`.
- **Проверка:** собранный через редактор установщик содержит актуальный маркер `TEST_МАРКЕР_123`. ✅ Подтверждено.


### FEATURE — удалённое обновление контента плеера (онлайн) <!-- REMOTE-UPDATE-LOGGED -->
- **Что:** плеер периодически опрашивает сервер на новую версию проекта и применяет обновление контента без переустановки .exe.
- **Логика плеера** (`packages/player/electron/main.js`):
  - `checkProjectVersion` — GET `/api/projects/:id/version` с player-токеном; при `serverVersion > knownVersion` шлёт `update-available` в renderer.
  - `startVersionPolling` — первая проверка через 10 сек после старта (только инициализирует `knownVersion`, без баннера), далее каждые 5 мин (`VERSION_POLL_INTERVAL`).
  - `apply-update` (IPC) — берёт `projectData` из ответа `/version`, разворачивает в корень `currentProject` (+`serverUrl`,`licenseKeyHash`), шлёт `load-project` + `update-applied`. Маркер: `APPLY-UPDATE-PROJECTDATA-V2`.
- **Серверная часть** (`packages/server/src/controllers/ProjectController.ts`):
  - `getProjectVersion` (роут `/:id/version`, middleware `authenticatePlayer`) теперь возвращает `projectData` + `canvasWidth/Height/Background`. Маркер: `VERSION-PROJECTDATA-APPLIED`.
  - Это убрало второй HTTP-запрос на `/api/projects/:id` (тот защищён `authenticateClient` = токен редактора, плееру недоступен → возвращал 401).
- **Причина прошлой поломки:** `apply-update` делал второй запрос на `/api/projects/:id` (401 для плеера) и неправильно распаковывал ответ (`{success, project:{projectData}}` вместо плоского `{widgets, canvas}` как в `project.json`).
- **Проверка:** на Windows-устройстве — баннер «Доступно обновление», нажатие «Обновить» сменило текст кнопки `TEST_МАРКЕР_123` → `TEST_МАРКЕР_456` без переустановки. ✅ Подтверждено.
- **Файлы:** `packages/player/electron/main.js`, `packages/server/src/controllers/ProjectController.ts`.

### KNOWN ISSUE — задержка детекта обновления и serverUrl в репо-копии project.json
- Баннер появляется не сразу: первая проверка (через 10 сек) только запоминает текущую версию, реальный детект — на следующем цикле (+5 мин). Можно уменьшить интервал или сразу детектить при первом расхождении с версией из `project.json`.
- `packages/player/electron/project.json` в репо содержит `serverUrl: :8080` (артефакт прошлой сборки) — на собранных через редактор плеерах подставляется корректный URL из `PLAYER_SERVER_URL`.
- TODO осталось: офлайн-кэш медиафайлов (сейчас контент обновляется онлайн, медиа тянутся с сервера по URL).

### FEATURE — офлайн-кэш медиафайлов плеера <!-- OFFLINE-CACHE-LOGGED -->
- **Что:** плеер скачивает медиафайлы проекта в локальный кэш и воспроизводит их офлайн; сеть нужна только для активации, heartbeat и проверки обновлений.
- **Реализация** (`packages/player/electron/main.js`):
  - Кастомный протокол `kioskcache://<projectId8>/<fileId>`, зарегистрирован как privileged (`standard, secure, supportFetchAPI, stream, bypassCSP`) ДО `app.ready`.
  - `getCacheDir(projectId)` → `userData/media-cache/<projectId8>/`. Имя файла = `fileId` (без расширения). Это НЕ системный кэш, ОС его не чистит.
  - `downloadToCache` — качает файл с player-токеном, пишет в `.tmp` → `rename`. Таймаут 8 сек (`req.setTimeout`) чтобы офлайн не вис.
  - `prepareProjectForRender(project)` — находит URL `/api/projects/<pid>/files/<fileId>` regex'ом, скачивает только отсутствующие в кэше, подменяет на `kioskcache://`. При офлайне/без токена пропускает сеть и использует кэш. Лог: `[cache] replaced N of M URLs with kioskcache://`.
  - `sendLoadProject()` — централизованная отправка проекта в renderer с кэшированием. Заменила 5 прямых `send('load-project')`.
  - Обработчик протокола `protocol.handle('kioskcache')` отдаёт файл с **поддержкой HTTP Range** (статус 206, `Content-Range`, `Accept-Ranges`) — критично для `<video>` seeking/буферизации. `guessMime` определяет MIME по расширению или магическим байтам (mp4/mov→`ftyp`, png/jpeg/gif/svg). `nodeStreamToWeb` через `Readable.toWeb`.
- **Решённые баги в процессе:**
  - Видео обрывалось на ~трети → `net.fetch('file://')` не поддерживает Range → заменено на ручную отдачу с Range. Маркер: `KIOSKCACHE-RANGE-SUPPORT`.
  - Окно активации не закрывалось после успеха → окно с `closable:false`, `close()` игнорируется в Electron 39 → `setClosable(true)` + `destroy()`. Маркер: `FIX-ACTIVATION-CLOSE-DESTROY`.
  - Видео не играло при первом запуске → кэширование шло до активации (без токена, 401) → добавлен `sendLoadProject()` после успешной активации. Маркер: `POST-ACTIVATION-CACHE`.
  - Офлайн-зависание → таймаут на download + пропуск сети если файл уже в кэше. Маркер: `CACHE-OFFLINE-FIX`.
  - Двойное скачивание при старте → убран вызов `sendLoadProject` из `loadEmbeddedProject` (остался только `did-finish-load`). Маркер: `FIX-DOUBLE-CACHE-CALL`.
- **Замечание по фото:** изображения в проекте — inline `data:image/...;base64` (часть `project.json`), кэшировать не нужно, работают офлайн всегда. Кэшируются только серверные файлы (`/api/.../files/...`): видео, SVG.
- **Серверный фикс:** multer в `builds.js` имел дефолтный лимит текстового поля 1 МБ → при сборке с тяжёлыми медиа `projectData` JSON превышал его (`MulterError: LIMIT_FIELD_VALUE`) → добавлен `fieldSize: 100MB`. Маркер: `MULTER-FIELDSIZE-FIX`. Файлы: `packages/server/src/routes/builds.js` + `dist`.
- **Проверка:** на Windows-устройстве видео воспроизводится онлайн и офлайн, файлы в `media-cache/`. ✅ Подтверждено.

### KNOWN ISSUE — мелочи офлайн-кэша
- При первом запуске видео появляется только после активации (нужен токен для скачивания) — ожидаемо.
- WS/heartbeat в офлайне спамят `EHOSTUNREACH` каждые 30 сек — не критично, но можно глушить лог.
- Старые файлы в кэше не удаляются при смене контента (нет сборки мусора кэша) — TODO на будущее.
