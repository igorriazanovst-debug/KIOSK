# ⚡ Quick Start - Kiosk Player

## 🚀 Быстрая сборка установщика

### Шаг 1: Установка

```bash
cd packages/player
npm install
```

### Шаг 2: (Опционально) Замените проект

Если хотите встроить свой проект:

```bash
# Скопируйте ваш экспортированный проект
copy "C:\путь\к\вашему\проекту.json" electron\project.json
```

Или используйте тестовый проект (уже включён).

### Шаг 3: Сборка

```bash
npm run electron:build:win
```

⏱️ Время сборки: ~5-10 минут

### Шаг 4: Результат

```
dist-electron\
  ├── Kiosk Player Setup.exe    ← Ваш установщик! (~80 MB)
  └── win-unpacked\
      └── Kiosk Player.exe      ← Portable версия
```

## 📦 Установка и запуск

1. Найдите `Kiosk Player Setup.exe`
2. Запустите установщик
3. Следуйте инструкциям
4. Запустите "Kiosk Player"

## ✅ Что дальше?

- **F11** - полноэкранный режим
- **ESC** - выход
- Протестируйте все кнопки и действия

## ❌ Исправление ошибок

### Ошибка: "Missing script: build:player"

**Причина:** Неправильная команда

**Решение:** Используйте:
```bash
npm run electron:build:win
```

### Ошибка: Cannot find module

**Решение:**
```bash
npm install
npm run build
npm run electron:build:win
```

### Ошибка при сборке TypeScript

**Решение:**
```bash
npm run build
# Проверьте что dist/ создана
dir dist
```

## 📝 Полная последовательность

```bash
# 1. Перейдите в папку player
cd packages/player

# 2. Установите зависимости
npm install

# 3. (Опционально) Замените проект
copy "your-project.json" electron\project.json

# 4. Соберите установщик
npm run electron:build:win

# 5. Найдите результат
dir dist-electron
```

## 🎯 Готово!

Ваш файл: `dist-electron\Kiosk Player Setup.exe`

Размер: ~80 MB  
Платформа: Windows x64  
Требования: Windows 7 SP1+
