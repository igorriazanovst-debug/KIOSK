# ✨ FEATURE v2.1.0: Формы обрезки изображений

## 🎯 Обзор

Виджет "Изображение" теперь поддерживает **9 форм обрезки**! Изображения могут быть обрезаны по различным геометрическим формам без потери качества.

---

## 🔷 Доступные формы

### 1. ⬜ Прямоугольник (rectangle)
- По умолчанию
- Без обрезки
- Стандартная прямоугольная форма

### 2. ⚫ Круг (circle)
- Идеальный круг
- Вписывается в меньшую сторону виджета
- `clip-path: circle(50% at 50% 50%)`

### 3. 🔴 Эллипс (ellipse)
- Овальная форма
- Заполняет всю область виджета
- `clip-path: ellipse(50% 50% at 50% 50%)`

### 4. 🔺 Треугольник (triangle)
- Равносторонний треугольник
- Вершина вверх
- `clip-path: polygon(50% 0%, 100% 100%, 0% 100%)`

### 5. 🔶 Ромб (diamond)
- Квадрат повёрнутый на 45°
- 4 вершины
- `clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)`

### 6. ⬢ Пятиугольник (pentagon)
- Правильный пятиугольник
- 5 равных сторон
- Вершина вверх

### 7. ⬡ Шестиугольник (hexagon)
- Правильный шестиугольник
- 6 равных сторон
- Как соты

### 8. ⭐ Восьмиугольник (octagon)
- Правильный восьмиугольник
- 8 равных сторон
- Как знак STOP

### 9. 🏷️ Закругленный прямоугольник (rounded-rectangle)
- Прямоугольник с закругленными углами
- Настраиваемый радиус углов (0-100px)
- `border-radius: Npx`

---

## 🎨 Как использовать

### В редакторе:

1. **Добавьте виджет "Изображение"**
2. **Загрузите изображение** (файл или URL)
3. **Откройте Properties** (правая панель)
4. **Найдите секцию "Форма обрезки"**
5. **Выберите форму** из выпадающего списка
6. **Для rounded-rectangle:** настройте радиус углов

```
┌─ Форма обрезки ─────────────┐
│ Форма: [Круг ▼]             │
└─────────────────────────────┘

Или:

┌─ Форма обрезки ─────────────┐
│ Форма: [Закругл. прям. ▼]  │
│ Радиус углов: [20] px       │
└─────────────────────────────┘
```

### В Preview/Player:

- Формы автоматически применяются
- Используется CSS `clip-path`
- Высокая производительность
- Работает во всех современных браузерах

---

## 🔧 Технические детали

### Редактор (Canvas):

**Компонент:** `ClippedImage.tsx`

**Технология:** HTML5 Canvas API

**Процесс:**
1. Создаётся временный canvas
2. Рисуется маска нужной формы через `ctx.clip()`
3. Изображение рисуется внутри маски
4. Canvas конвертируется в `data:URL`
5. Применяется к Konva Image

**Код:**
```typescript
// Пример для круга
ctx.beginPath();
ctx.arc(centerX, centerY, radius, 0, Math.PI * 2);
ctx.closePath();
ctx.clip();
ctx.drawImage(image, drawX, drawY, drawWidth, drawHeight);
```

### Preview/Player:

**Технология:** CSS `clip-path`

**Процесс:**
1. Вычисляется CSS `clip-path` для формы
2. Применяется к контейнеру div
3. Браузер обрезает изображение аппаратно

**Код:**
```typescript
// Пример для треугольника
style.clipPath = 'polygon(50% 0%, 100% 100%, 0% 100%)';

// Для закругленного прямоугольника
style.borderRadius = '20px';
```

---

## 📐 Математика форм

### Многоугольники (pentagon, hexagon, octagon):

Вершины вычисляются по формуле:
```typescript
for (let i = 0; i < sides; i++) {
  const angle = (i * 2 * Math.PI / sides) - Math.PI / 2;
  const x = centerX + radius * Math.cos(angle);
  const y = centerY + radius * Math.sin(angle);
  points.push({ x, y });
}
```

**Угол поворота:** `-Math.PI / 2` (вершина вверх)

### Круг и эллипс:

**Круг:** радиус = `Math.min(width, height) / 2`  
**Эллипс:** radiusX = `width / 2`, radiusY = `height / 2`

---

## 🎯 Примеры использования

### Аватары пользователей
```
Форма: Круг
Размер: 200x200px
```

### Логотипы компаний
```
Форма: Шестиугольник
Размер: 150x150px
```

### Кнопки с иконками
```
Форма: Закругленный прямоугольник
Радиус: 12px
Размер: 120x40px
```

### Декоративные элементы
```
Форма: Треугольник / Ромб
Размер: 100x100px
```

---

## 🚀 Производительность

### Редактор (Konva):
- ⚡ Canvas клиппинг выполняется один раз при загрузке
- 📦 Результат кэшируется
- 🎨 Перерисовка только при изменении формы/размера

### Preview/Player (CSS):
- ⚡⚡⚡ Аппаратное ускорение через GPU
- 📦 Браузер кэширует clip-path
- 🎨 Нет JavaScript вычислений в runtime

**Вывод:** Нет влияния на производительность! ✅

---

## 🎨 Сочетание с другими функциями

### ✅ Работает с:
- Все типы заполнения (contain, cover, fill, scale-down)
- Рамки (border)
- Поворот (rotation)
- Z-index (наслоение)
- Привязка к сетке

### ⚠️ Особенности:
- Рамка рисуется **поверх** обрезанного изображения
- При повороте форма поворачивается вместе с виджетом
- objectFit применяется **до** клиппинга

---

## 📋 Структура данных

### Widget properties:

```typescript
interface ImageWidgetProperties {
  src: string;
  objectFit: 'contain' | 'cover' | 'fill' | 'scale-down' | 'adaptive';
  
  // НОВЫЕ СВОЙСТВА
  clipShape: 
    | 'rectangle' 
    | 'circle' 
    | 'ellipse' 
    | 'triangle' 
    | 'diamond' 
    | 'pentagon' 
    | 'hexagon' 
    | 'octagon' 
    | 'rounded-rectangle';
  cornerRadius: number; // Только для rounded-rectangle (0-100)
  
  // Существующие
  borderEnabled: boolean;
  borderWidth: number;
  borderColor: string;
  opacity: number;
}
```

### Пример JSON:

```json
{
  "id": "widget-123",
  "type": "image",
  "x": 100,
  "y": 100,
  "width": 200,
  "height": 200,
  "properties": {
    "src": "https://example.com/avatar.jpg",
    "objectFit": "cover",
    "clipShape": "circle",
    "borderEnabled": true,
    "borderWidth": 4,
    "borderColor": "#007acc"
  }
}
```

---

## 🧪 Тестирование

### Тест 1: Все формы работают
```
1. Создайте 9 Image виджетов
2. Загрузите одно изображение
3. Примените каждую форму
4. Проверьте в Preview
✅ Все формы должны отображаться правильно
```

### Тест 2: Радиус углов
```
1. Создайте Image виджет
2. Выберите форму "Закругленный прямоугольник"
3. Измените радиус: 0, 10, 20, 50, 100
✅ Углы должны изменяться плавно
```

### Тест 3: Совместимость
```
1. Примените форму "Круг"
2. Включите рамку
3. Поверните виджет на 45°
4. Измените objectFit на "cover"
✅ Всё должно работать вместе
```

### Тест 4: Player
```
1. Создайте проект с разными формами
2. Экспортируйте в Player
3. Соберите установщик
4. Запустите Player
✅ Все формы работают как в редакторе
```

---

## 📱 Поддержка браузеров

### CSS clip-path:
- ✅ Chrome 55+
- ✅ Firefox 54+
- ✅ Safari 9.1+
- ✅ Edge 79+

**Совместимость:** 98%+ пользователей

### Fallback:
Если браузер не поддерживает `clip-path`:
- Изображение показывается как прямоугольник
- Все функции работают
- Нет ошибок

---

## 💡 Советы

### 1. Выбор формы:
- **Аватары:** круг
- **Логотипы:** круг, шестиугольник
- **Кнопки:** закругленный прямоугольник
- **Декор:** треугольник, ромб, пятиугольник

### 2. Размер изображения:
- Используйте квадратные изображения для симметричных форм
- Для треугольника: изображение должно быть выше
- Для ромба: квадратное изображение идеально

### 3. objectFit:
- **cover** - для заполнения формы без пустот
- **contain** - для показа всего изображения

### 4. Рамки:
- Рамка рисуется поверх формы
- Для круглых форм используйте тонкие рамки (2-4px)
- Цвет рамки контрастный к изображению

---

## 🔮 Будущие улучшения

### Возможные дополнения:
- ⭐ Звезда (5, 6, 8 лучей)
- 💗 Сердце
- ☁️ Облако
- 🔥 Произвольная SVG маска
- 🎭 Несколько форм одновременно

---

## 📚 Связанные функции

- **Gallery mode** - (следующее сообщение)
- **Video clip shapes** - (будущая функция)
- **Animated transitions** - (будущая функция)

---

## ✅ Changelog

### v2.1.0
- ✅ Добавлено 9 форм обрезки
- ✅ ClippedImage компонент
- ✅ UI в Properties Panel
- ✅ Поддержка в Preview/Player
- ✅ CSS clip-path для высокой производительности

---

**Версия:** 2.1.0  
**Дата:** Декабрь 2025  
**Статус:** ✅ Готово

🎉 **Формы обрезки работают отлично!**
