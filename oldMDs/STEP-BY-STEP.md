# 📋 Пошаговая инструкция: От проекта до установщика

## 🎯 Цель
Создать установочный файл `.exe` для Windows, который содержит ваш киоск-проект.

---

## 📝 Шаг 1: Создайте проект в редакторе

### 1.1 Запустите редактор
```bash
# Из корневой папки kiosk-content-platform
npm run dev:editor
```

### 1.2 Создайте контент
- Добавьте виджеты (кнопки, текст, изображения, видео, меню)
- Настройте их свойства
- Добавьте действия к кнопкам и меню
- Протестируйте в режиме Preview

### 1.3 Сохраните проект
1. Нажмите кнопку **"💾 Сохранить"** в Toolbar
2. Выберите место сохранения
3. Введите имя файла (например: `my-kiosk-project.json`)
4. Нажмите "Сохранить"

**✅ Результат:** У вас есть файл `my-kiosk-project.json`

**📍 Запомните где сохранили файл!** Например: `C:\Users\YourName\Downloads\my-kiosk-project.json`

---

## 📦 Шаг 2: Встройте проект в Player

### 2.1 Найдите папку Player
```bash
# Из корневой папки перейдите в player
cd packages/player
```

### 2.2 Замените тестовый проект на ваш

**Windows (PowerShell или CMD):**
```cmd
copy "C:\Users\YourName\Downloads\my-kiosk-project.json" electron\project.json
```

**Windows (если путь с пробелами):**
```cmd
copy "C:\Users\Your Name\Downloads\my-kiosk-project.json" electron\project.json
```

**Альтернатива - через Проводник:**
1. Откройте папку `packages\player\electron\`
2. Найдите файл `project.json`
3. Замените его на ваш `my-kiosk-project.json`
4. Переименуйте файл в `project.json`

**✅ Результат:** Файл `packages/player/electron/project.json` содержит ваш проект

---

## 🔨 Шаг 3: Установите зависимости Player (один раз)

### 3.1 Убедитесь что вы в папке player
```bash
# Если вы в корне проекта:
cd packages/player

# Если вы уже в packages/player:
# Пропустите эту команду
```

### 3.2 Установите зависимости
```bash
npm install
```

⏱️ **Время:** 2-5 минут  
📦 **Размер:** ~300 MB node_modules

### 3.3 Установите дополнительные зависимости для сборки
```bash
npm install 7zip-bin app-builder-bin --save-dev
```

⏱️ **Время:** 30-60 секунд  
📦 **Важно:** Эти пакеты нужны для создания установщика Windows

**✅ Результат:** Папка `node_modules` создана, все зависимости установлены

**💡 Эти команды нужно выполнить только один раз!** При следующих сборках можно пропустить.

---

## 🚀 Шаг 4: Создайте установщик

### 4.1 Соберите Player с вашим проектом

**Из папки player:**
```bash
npm run electron:build:win
```

**ИЛИ из корневой папки:**
```bash
npm run player:build
```

⏱️ **Время:** 5-10 минут (первая сборка), 2-3 минуты (последующие)

**Что происходит:**
1. ✅ Компиляция TypeScript
2. ✅ Сборка React приложения (Vite)
3. ✅ Упаковка в Electron
4. ✅ Создание установщика NSIS

**Вы увидите в консоли:**
```
building        target=nsis arch=x64
packaging       platform=win32 arch=x64 electron=28.x.x
• building      block map  blockMapFile=...
• building      nsis installer  
```

**✅ Результат:** Установщик создан!

---

## 📥 Шаг 5: Найдите установщик

### 5.1 Откройте папку с результатом
```bash
# Из packages/player
cd dist-electron

# ИЛИ через проводник
# Перейдите в packages\player\dist-electron\
```

### 5.2 Найдите файлы

**📦 Установщик (для распространения):**
```
Kiosk Player Setup 1.0.0.exe    (~80-100 MB)
```

**📂 Portable версия (без установки):**
```
win-unpacked\
  └── Kiosk Player.exe
```

**✅ Результат:** У вас есть два варианта запуска

---

## 🎮 Шаг 6: Протестируйте установщик

### 6.1 Запустите установщик
1. Двойной клик на `Kiosk Player Setup 1.0.0.exe`
2. Выберите папку установки (или оставьте по умолчанию)
3. Нажмите "Install"
4. Дождитесь окончания установки

### 6.2 Запустите приложение
- **Из меню Пуск:** Найдите "Kiosk Player"
- **С рабочего стола:** Двойной клик на ярлык
- **Portable:** Запустите `win-unpacked\Kiosk Player.exe`

### 6.3 Проверьте работу
1. ✅ Приложение открылось
2. ✅ Ваш проект загрузился
3. ✅ Все виджеты на месте
4. ✅ Кнопки работают
5. ✅ Действия выполняются
6. ✅ F11 → полноэкранный режим работает

**✅ Результат:** Всё работает! Готово к использованию.

---

## 🎁 Шаг 7: Распространение

### 7.1 Скопируйте установщик

**Файл для распространения:**
```
packages\player\dist-electron\Kiosk Player Setup 1.0.0.exe
```

### 7.2 Варианты доставки

**💾 USB флешка:**
- Скопируйте .exe на флешку
- Отнесите на целевой компьютер

**☁️ Облако:**
- Загрузите в Google Drive / Dropbox / OneDrive
- Отправьте ссылку клиенту

**📧 Email:**
- Прикрепите файл (если размер позволяет)
- Или ссылку на облачное хранилище

**🌐 Файлообменник:**
- WeTransfer, Mega, etc.

**✅ Результат:** Клиент получил установщик

---

## 📊 Полная схема процесса

```
1. Редактор
   └── Создать проект → Сохранить .json
                              ↓
2. Копирование
   └── my-kiosk.json → packages/player/electron/project.json
                              ↓
3. Сборка
   └── npm run player:build
                              ↓
4. Результат
   └── dist-electron/Kiosk Player Setup.exe
                              ↓
5. Распространение
   └── Отправить клиенту → Установить → Готово!
```

---

## ⚡ Быстрая справка команд

```bash
# ШАГ 1: Создать проект
npm run dev:editor
# → Создайте и сохраните проект

# ШАГ 2: Скопировать проект
cd packages/player
copy "путь\к\проекту.json" electron\project.json

# ШАГ 3: Установить зависимости (один раз)
npm install
npm install 7zip-bin app-builder-bin --save-dev

# ШАГ 4: Собрать установщик
npm run electron:build:win

# ШАГ 5: Найти результат
cd dist-electron
dir
# → Найдите Kiosk Player Setup.exe
```

---

## 🔄 Обновление проекта

Если вы изменили проект и хотите пересобрать:

```bash
# 1. Откройте редактор, измените, сохраните
npm run dev:editor

# 2. Замените project.json
cd packages/player
copy "новый-проект.json" electron\project.json

# 3. Пересоберите (зависимости уже установлены!)
npm run electron:build:win

# 4. Новый установщик готов
cd dist-electron
```

⏱️ **Время пересборки:** 2-3 минуты (быстрее, чем первый раз)

**Примечание:** Если возникают ошибки, может потребоваться переустановить зависимости:
```bash
npm install 7zip-bin app-builder-bin --save-dev
```

---

## ❓ Частые вопросы

### Q: Нужно ли каждый раз устанавливать зависимости?
**A:** Нет! Только один раз. При следующих сборках пропустите `npm install`.

### Q: Где находится мой сохранённый проект?
**A:** Там, куда вы его сохранили через кнопку "Сохранить" в редакторе. Обычно в папке "Загрузки" или "Документы".

### Q: Что делать если забыл где сохранил проект?
**A:** 
1. Откройте редактор: `npm run dev:editor`
2. Нажмите "Открыть" 
3. Найдите проект в списке недавних файлов
4. Сохраните снова в известное место

### Q: Можно ли протестировать без создания установщика?
**A:** Да! Запустите dev режим:
```bash
cd packages/player
npm run electron:dev
```

### Q: Как изменить название приложения?
**A:** Отредактируйте `packages/player/package.json` → `"productName": "Ваше название"`

### Q: Установщик слишком большой (80+ MB). Это нормально?
**A:** Да! Установщик включает:
- Electron (Chromium) ~70 MB
- Ваше приложение ~5-10 MB
- Ваш проект с ресурсами

### Q: Нужен ли интернет для работы установленного приложения?
**A:** Нет! Приложение работает полностью offline (если в проекте нет ссылок на внешние ресурсы).

---

## 🐛 Решение проблем

### Ошибка: "command not found: npm"
**Решение:** Установите Node.js с https://nodejs.org/

### Ошибка: "Cannot find module"
**Решение:** 
```bash
cd packages/player
npm install
```

### Ошибка при копировании проекта
**Решение:** Используйте абсолютный путь с кавычками:
```bash
copy "C:\Full\Path\To\project.json" electron\project.json
```

### Установщик не создаётся
**Проверьте:**
1. ✅ `npm install` выполнен
2. ✅ `npm install 7zip-bin app-builder-bin --save-dev` выполнен
3. ✅ Файл `electron/project.json` существует
4. ✅ Запустите: `npm run build` → потом `npm run electron:build:win`

### Ошибка: "Cannot find module app-builder-bin"
**Решение:**
```bash
cd packages/player
npm install 7zip-bin app-builder-bin --save-dev
npm run electron:build:win
```

### Ошибка: "Cannot create symbolic link" (Windows)
**Решение:** Отключите подписывание кода (уже сделано в v2.0.1)
```bash
# Очистите кэш
rmdir /s /q "%LOCALAPPDATA%\electron-builder\Cache"

# Соберите снова
npm run electron:build:win
```

### Player не загружает проект
**Проверьте:**
1. Файл `electron/project.json` валидный JSON
2. Откройте файл в текстовом редакторе - должна быть структура проекта
3. Пересоберите: `npm run electron:build:win`

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте что выполнили все шаги
2. Перечитайте раздел "Решение проблем"
3. Посмотрите логи в консоли - там обычно видна причина ошибки

---

## ✅ Чеклист готовности

Перед распространением проверьте:

- [ ] Проект создан и сохранён в редакторе
- [ ] Проект скопирован в `packages/player/electron/project.json`
- [ ] Зависимости установлены (`npm install`)
- [ ] Установщик собран (`npm run electron:build:win`)
- [ ] Установщик найден в `dist-electron/`
- [ ] Установщик протестирован на тестовом компьютере
- [ ] Все виджеты работают
- [ ] Все действия выполняются
- [ ] Полноэкранный режим работает (F11)

---

**🎉 Готово! Теперь у вас есть готовый установщик для Windows!**

**Версия инструкции:** 1.0  
**Дата:** Декабрь 2025
