# 🔧 Исправление v1.9.2 - Точное позиционирование в Preview

## 🐛 Проблема

При открытии режима превью виджеты смещались и налазили друг на друга. Позиции виджетов в превью не соответствовали их расположению в редакторе.

## 🔍 Причина

**CSS Box Model:**
По умолчанию браузер использует `box-sizing: content-box`, что означает:
- `width` и `height` определяют размер только содержимого
- `padding` добавляется к размеру
- `border` добавляется к размеру

**Пример проблемы:**
```
Виджет в редакторе: 200×100px
Виджет в Preview с padding: 20px
Виджет с border: 5px

Реальный размер БЕЗ box-sizing:
200 + (20×2 padding) + (5×2 border) = 250×150px
❌ Виджет стал БОЛЬШЕ!
```

## ✅ Решение

Добавлено `box-sizing: border-box` для всех виджетов в Preview.

**С box-sizing: border-box:**
- `width` и `height` включают всё: content + padding + border
- Размер виджета всегда точный
- Позиция не смещается

**Пример исправления:**
```
Виджет в редакторе: 200×100px
Виджет в Preview с padding: 20px
Виджет с border: 5px

Реальный размер С box-sizing:
200×100px (padding и border ВНУТРИ)
✅ Размер точный!
```

## 📝 Изменения в коде

### 1. Добавлено box-sizing в commonStyle

**Файл:** `Preview.tsx`

**Было:**
```typescript
const commonStyle: React.CSSProperties = {
  position: 'absolute',
  left: widget.x,
  top: widget.y,
  width: widget.width,
  height: widget.height,
  transform: widget.rotation ? `rotate(${widget.rotation}deg)` : undefined,
  zIndex: widget.zIndex || 0
};
```

**Стало:**
```typescript
const commonStyle: React.CSSProperties = {
  position: 'absolute',
  left: widget.x,
  top: widget.y,
  width: widget.width,
  height: widget.height,
  transform: widget.rotation ? `rotate(${widget.rotation}deg)` : undefined,
  zIndex: widget.zIndex || 0,
  boxSizing: 'border-box' // ← ДОБАВЛЕНО
};
```

### 2. Исправлена обработка transform в кнопке

**Проблема:** При нажатии на кнопку `transform` перезаписывался, теряя `rotate`.

**Было:**
```typescript
onMouseDown={(e) => {
  e.currentTarget.style.transform = 'scale(0.95)';
}}
onMouseUp={(e) => {
  e.currentTarget.style.transform = 'scale(1)';
}}
```

**Стало:**
```typescript
const handleMouseDown = (e: React.MouseEvent<HTMLButtonElement>) => {
  const rotation = widget.rotation ? `rotate(${widget.rotation}deg) ` : '';
  e.currentTarget.style.transform = `${rotation}scale(0.95)`;
};

const handleMouseUp = (e: React.MouseEvent<HTMLButtonElement>) => {
  const rotation = widget.rotation ? `rotate(${widget.rotation}deg) ` : '';
  e.currentTarget.style.transform = `${rotation}scale(1)`;
};
```

## 🧪 Как проверить исправление

### Тест 1: Кнопка с padding

**Шаги:**
1. Создайте кнопку 200×60
2. Установите padding: 16px
3. Разместите на холсте
4. Откройте превью
5. ✅ Кнопка на том же месте
6. ✅ Размер точный 200×60

### Тест 2: Текст с padding и border

**Шаги:**
1. Создайте текст 300×100
2. Установите padding: 20px
3. Добавьте border: 5px
4. Разместите рядом с другим виджетом
5. Откройте превью
6. ✅ Виджеты не налезают
7. ✅ Позиции совпадают

### Тест 3: Несколько виджетов рядом

**Шаги:**
1. Создайте 3 кнопки подряд
   - Кнопка 1: x=100, width=150
   - Кнопка 2: x=250, width=150
   - Кнопка 3: x=400, width=150
2. У всех padding: 16px
3. Откройте превью
4. ✅ Кнопки не налезают
5. ✅ Все на своих местах

### Тест 4: Повёрнутая кнопка

**Шаги:**
1. Создайте кнопку
2. Поверните на 45°
3. Откройте превью
4. Нажмите на кнопку (анимация scale)
5. ✅ Поворот сохраняется при нажатии
6. ✅ Позиция не меняется

### Тест 5: Меню с границами

**Шаги:**
1. Создайте меню
2. Установите border: 2px
3. Разместите на холсте
4. Откройте превью
5. ✅ Меню точно на месте
6. ✅ Размер не изменился

## 📊 Визуальное объяснение

### Box-sizing: content-box (default - было)

```
┌─────────────────────────────┐
│       border (5px)          │
│  ┌─────────────────────┐    │
│  │   padding (20px)    │    │
│  │  ┌──────────────┐   │    │
│  │  │   content    │   │    │ ← width: 200px
│  │  │   200×100    │   │    │ ← height: 100px
│  │  └──────────────┘   │    │
│  └─────────────────────┘    │
└─────────────────────────────┘
Итого: 250×150px ❌
```

### Box-sizing: border-box (стало)

```
┌─────────────────────────┐
│   border (5px)          │
│ ┌─────────────────────┐ │
│ │  padding (20px)     │ │
│ │ ┌───────────────┐   │ │
│ │ │   content     │   │ │ ← ВЕСЬ блок: 200×100
│ │ │   150×60      │   │ │
│ │ └───────────────┘   │ │
│ └─────────────────────┘ │
└─────────────────────────┘
Итого: 200×100px ✅
```

## 🎯 Затронутые виджеты

**Все виджеты получили исправление:**
- ✅ Shape (фигура)
- ✅ Text (текст)
- ✅ Button (кнопка)
- ✅ Image (изображение)
- ✅ Video (видео)
- ✅ Menu (меню)

## 📦 Технические детали

**CSS box-sizing:**
```css
box-sizing: border-box;
```

**Влияние на размеры:**
- `width` и `height` теперь включают padding и border
- Content area уменьшается соответственно
- Общий размер виджета остаётся точным

**Совместимость:**
- Работает во всех современных браузерах
- IE8+ (с префиксом)
- Стандарт CSS3

## 📥 Обновление

- [⬇️ kiosk-content-platform.zip](computer:///mnt/user-data/outputs/kiosk-content-platform.zip) (80 KB)
- [⬇️ kiosk-content-platform.tar.gz](computer:///mnt/user-data/outputs/kiosk-content-platform.tar.gz) (52 KB)

---

## 🚀 Дополнительные улучшения

**Также исправлено:**
- Сохранение rotation при анимации кнопок
- Точная обработка transform в hover-эффектах
- Корректная работа всех CSS-свойств

**Результат:**
- ✅ Pixel-perfect позиционирование
- ✅ Точное соответствие редактору
- ✅ Нет наложений виджетов
- ✅ Все размеры корректны

---

**Версия:** 1.9.2  
**Дата:** Декабрь 2025  
**Статус:** ✅ Позиционирование исправлено полностью
