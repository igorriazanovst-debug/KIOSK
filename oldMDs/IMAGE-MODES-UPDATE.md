# 🖼️ Обновлённые режимы отображения изображений

## 🎉 Что изменилось

### 1. ✅ Исправлен режим "Оригинальный размер"

**Было:** Показывал изображение в оригинальном размере, обрезая если не влезало

**Стало:** Сохраняет оригинальные пропорции и масштабирует так, чтобы всё изображение вписалось в виджет

**Технически:** 
- Старое название: `none` → Новое: `scale-down`
- Если изображение меньше виджета - показывает как есть (не увеличивает)
- Если изображение больше виджета - уменьшает с сохранением пропорций

---

### 2. ✨ Новый режим "Адаптивный"

**Что делает:** Автоматически изменяет пропорции виджета под пропорции изображения

**Как работает:**
1. Загружаете изображение
2. Выбираете режим "Адаптивный"
3. ✅ Виджет автоматически меняет высоту, сохраняя ширину
4. ✅ Пропорции виджета = пропорции изображения

**Технически:**
- Значение: `adaptive`
- Сохраняется ширина виджета
- Высота пересчитывается: `height = width / (img.width / img.height)`
- Обновление происходит автоматически при загрузке изображения или смене режима

---

## 📋 Полный список режимов

| Режим | Значение | Описание |
|-------|----------|----------|
| **Пропорционально (вписать)** | `contain` | Вписывает всё изображение, сохраняя пропорции. Могут быть пустые поля |
| **Заполнить весь виджет** | `cover` | Заполняет виджет полностью, обрезая лишнее |
| **Растянуть** | `fill` | Растягивает на весь виджет, игнорируя пропорции |
| **Оригинальный размер** | `scale-down` | Сохраняет пропорции, масштабирует только если больше виджета |
| **Адаптивный** | `adaptive` | **НОВЫЙ!** Меняет размер виджета под пропорции изображения |

---

## 🎯 Когда использовать какой режим

### Пропорционально (contain)
**Используйте когда:** Нужно показать всё изображение целиком
- ✅ Логотипы
- ✅ Иконки
- ✅ Фотографии для галереи
- ❌ Могут быть пустые поля по бокам

### Заполнить весь виджет (cover)
**Используйте когда:** Нужно заполнить область без пустот
- ✅ Фоновые изображения
- ✅ Баннеры
- ✅ Заставки
- ❌ Края изображения обрезаются

### Растянуть (fill)
**Используйте когда:** Изображение должно заполнить виджет полностью
- ✅ Паттерны и текстуры
- ✅ Абстрактные фоны
- ❌ Искажает пропорции

### Оригинальный размер (scale-down)
**Используйте когда:** Важна точность размера
- ✅ Скриншоты с текстом
- ✅ Диаграммы и графики
- ✅ QR-коды
- ❌ Может быть слишком маленьким или большим

### Адаптивный (adaptive) 🆕
**Используйте когда:** Виджет должен подстроиться под изображение
- ✅ Вы не знаете заранее пропорции изображения
- ✅ Изображение задаёт размеры блока
- ✅ Адаптивные карточки товаров
- ✅ Динамический контент
- ❌ Виджет изменит свои размеры автоматически

---

## 🧪 Как проверить

### Тест 1: Оригинальный размер

1. Добавьте виджет "Изображение" размером 400×300
2. Загрузите большое изображение (например, 1920×1080)
3. Выберите режим "Оригинальный размер"
4. ✅ Всё изображение должно вписаться в виджет 400×300
5. ✅ Пропорции сохранены (нет искажений)
6. ✅ Может быть пустое место по бокам или сверху/снизу

### Тест 2: Адаптивный режим

**Сценарий A: Горизонтальное изображение (широкое)**

1. Добавьте виджет размером 300×200
2. Загрузите широкую картинку (например, 1920×1080, соотношение 16:9)
3. Выберите режим "Адаптивный"
4. ✅ Ширина остаётся 300
5. ✅ Высота автоматически станет ~169 (300 ÷ 16 × 9)
6. ✅ Виджет теперь 300×169

**Сценарий B: Вертикальное изображение (высокое)**

1. Виджет 300×200
2. Загрузите высокую картинку (например, 1080×1920, соотношение 9:16)
3. Выберите "Адаптивный"
4. ✅ Ширина остаётся 300
5. ✅ Высота автоматически станет ~533 (300 ÷ 9 × 16)
6. ✅ Виджет теперь 300×533

**Сценарий C: Квадратное изображение**

1. Виджет 300×200
2. Загрузите квадратную картинку (1000×1000, соотношение 1:1)
3. Выберите "Адаптивный"
4. ✅ Ширина остаётся 300
5. ✅ Высота автоматически станет 300
6. ✅ Виджет теперь 300×300 (квадрат)

### Тест 3: Переключение между режимами

1. Загрузите изображение 1920×1080 (16:9)
2. Виджет 400×300
3. Попробуйте все режимы:
   - **Пропорционально**: вписывается, есть пустые поля
   - **Заполнить**: заполняет весь виджет, обрезается
   - **Растянуть**: искажается под 400×300
   - **Оригинальный**: вписывается как "Пропорционально"
   - **Адаптивный**: виджет меняется на 400×225

---

## 💡 Технические детали

### Логика `scale-down` (Оригинальный размер)

```typescript
case 'scale-down': {
  // Если изображение меньше виджета - показываем как есть
  if (image.width <= width && image.height <= height) {
    cropWidth = image.width;
    cropHeight = image.height;
  } else {
    // Если больше - вписываем с пропорциями
    // crop остаётся полным (весь image)
  }
  break;
}
```

### Логика `adaptive` (Адаптивный)

```typescript
useEffect(() => {
  if (!image || widget.properties.objectFit !== 'adaptive') return;

  const imgRatio = image.width / image.height;
  const currentRatio = widget.width / widget.height;

  // Если пропорции различаются
  if (Math.abs(imgRatio - currentRatio) > 0.01) {
    // Сохраняем ширину, меняем высоту
    const newHeight = widget.width / imgRatio;
    
    updateWidget(widget.id, {
      height: Math.round(newHeight)
    });
  }
}, [image, widget.properties.objectFit, widget.width]);
```

**Особенности:**
- Погрешность 0.01 для избежания бесконечного цикла
- Высота округляется до целого числа
- Срабатывает при:
  - Загрузке изображения
  - Смене режима на "Адаптивный"
  - Изменении ширины виджета (высота пересчитывается)

---

## 🎨 Примеры использования

### Пример 1: Галерея товаров
```
Режим: Адаптивный
- Загружаете фото товара любых пропорций
- Виджет автоматически подстраивается
- Все карточки разных размеров, но без искажений
```

### Пример 2: Баннер на весь экран
```
Режим: Заполнить весь виджет
- Виджет 1920×300 (горизонтальный баннер)
- Изображение любого размера
- Баннер полностью заполнен, без пустот
```

### Пример 3: Логотип компании
```
Режим: Пропорционально (вписать)
- Виджет 200×100
- Логотип сохраняет пропорции
- Центрируется в виджете
```

### Пример 4: QR-код
```
Режим: Оригинальный размер
- QR-код 300×300 пикселей
- Виджет 500×500
- QR показывается 300×300 (не растягивается)
- Остаётся читаемым
```

---

## 📥 Обновление

- [⬇️ kiosk-content-platform.zip](computer:///mnt/user-data/outputs/kiosk-content-platform.zip) (45 KB) - для Windows
- [⬇️ kiosk-content-platform.tar.gz](computer:///mnt/user-data/outputs/kiosk-content-platform.tar.gz) (26 KB) - для Linux/macOS

---

**Версия:** 1.1.0  
**Дата:** Декабрь 2025  
**Статус:** ✅ Новые режимы работают
