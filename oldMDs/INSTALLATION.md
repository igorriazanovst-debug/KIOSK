# Установка Kiosk Content Platform из архива

## 📦 Доступные форматы

Проект доступен в двух форматах:
- `kiosk-content-platform.zip` (30 KB) - для Windows
- `kiosk-content-platform.tar.gz` (17 KB) - для Linux/Mac

## 🚀 Быстрая установка

### Windows

1. **Распакуйте архив:**
   ```cmd
   # Используйте проводник Windows или
   tar -xf kiosk-content-platform.zip
   ```

2. **Перейдите в директорию:**
   ```cmd
   cd kiosk-content-platform
   ```

3. **Установите зависимости:**
   ```cmd
   npm install
   ```
   
   Это может занять несколько минут при первом запуске.

4. **Запустите редактор:**
   ```cmd
   npm run dev:editor
   ```

5. **Откройте браузер:**
   ```
   http://localhost:3000
   ```

### Linux / macOS

1. **Распакуйте архив:**
   ```bash
   tar -xzf kiosk-content-platform.tar.gz
   ```

2. **Перейдите в директорию:**
   ```bash
   cd kiosk-content-platform
   ```

3. **Установите зависимости:**
   ```bash
   npm install
   ```

4. **Запустите редактор:**
   ```bash
   npm run dev:editor
   ```

5. **Откройте браузер:**
   ```
   http://localhost:3000
   ```

## ⚙️ Требования

### Необходимое ПО:
- **Node.js** версии 18.0 или выше
- **npm** версии 9.0 или выше

### Проверка версий:
```bash
node --version   # Должно быть v18.0.0 или выше
npm --version    # Должно быть 9.0.0 или выше
```

### Установка Node.js:
Если Node.js не установлен:
- Скачайте с официального сайта: https://nodejs.org/
- Выберите LTS (Long Term Support) версию
- Установите с настройками по умолчанию

## 📁 Структура проекта после распаковки

```
kiosk-content-platform/
├── packages/
│   └── editor/                 # Редактор контента
│       ├── src/                # Исходный код
│       ├── package.json
│       └── vite.config.ts
├── examples/
│   └── demo-project.kiosk.json # Пример проекта
├── README.md                   # Описание проекта
├── ARCHITECTURE.md             # Архитектура
├── QUICKSTART.md               # Руководство пользователя
├── package.json                # Главный package.json
└── .gitignore
```

## 🎯 Первые шаги

После успешного запуска:

1. **Изучите интерфейс**
   - Левая панель: библиотека виджетов
   - Центр: холст для редактирования
   - Правая панель: свойства виджета

2. **Добавьте первый виджет**
   - Кликните по виджету в левой панели
   - Он появится на холсте
   - Перетащите его в нужное место

3. **Измените свойства**
   - Выберите виджет на холсте
   - Настройте параметры в правой панели

4. **Сохраните проект**
   - Нажмите "Сохранить" на панели инструментов
   - Файл сохранится как `.kiosk.json`

5. **Загрузите пример**
   - Нажмите "Открыть"
   - Выберите `examples/demo-project.kiosk.json`

## 🔧 Решение проблем

### Ошибка: "npm: command not found"
**Решение:** Установите Node.js (включает npm)

### Ошибка: "EACCES: permission denied"
**Решение (Linux/Mac):**
```bash
sudo chown -R $USER ~/.npm
sudo chown -R $USER node_modules
```

### Ошибка: "Port 3000 is already in use"
**Решение:** Измените порт в `packages/editor/vite.config.ts`:
```typescript
server: {
  port: 3001, // Измените на другой порт
  open: true
}
```

### Медленная установка зависимостей
**Решение:** Используйте другое зеркало npm:
```bash
npm config set registry https://registry.npmjs.org/
```

### Браузер не открывается автоматически
**Решение:** Откройте вручную: `http://localhost:3000`

## 📚 Дополнительная документация

- `README.md` - Общее описание проекта
- `ARCHITECTURE.md` - Подробная техническая документация
- `QUICKSTART.md` - Руководство пользователя
- `SETUP.md` - Детальные инструкции по настройке

## 🎓 Обучающие материалы

### Горячие клавиши:
- `Ctrl/Cmd + Z` - Отменить
- `Ctrl/Cmd + Shift + Z` - Повторить
- `Ctrl/Cmd + C` - Копировать
- `Ctrl/Cmd + V` - Вставить
- `Ctrl/Cmd + X` - Вырезать
- `Delete` - Удалить виджет
- `Ctrl/Cmd + Click` - Множественное выделение

### Типы виджетов:
1. **Прямоугольник** - базовая фигура
2. **Кнопка** - интерактивная кнопка с текстом
3. **Текст** - текстовый блок
4. **Изображение** - отображение картинок
5. **Видео** - видеоплеер

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте версии Node.js и npm
2. Убедитесь, что порт 3000 свободен
3. Попробуйте удалить `node_modules` и запустить `npm install` снова
4. Проверьте консоль браузера на наличие ошибок

## ✅ Успешная установка

Если вы видите редактор в браузере с:
- Темной панелью инструментов вверху
- Библиотекой виджетов слева
- Белым холстом в центре
- Панелью свойств справа

**Поздравляем! Установка прошла успешно!** 🎉

Теперь вы можете начать создавать контент для киосков.

## 🔄 Обновление проекта

В будущем для обновления:
```bash
cd kiosk-content-platform
git pull origin main  # Если используете git
npm install           # Обновить зависимости
npm run dev:editor    # Запустить
```

## 📝 Следующие шаги

1. Прочитайте `QUICKSTART.md` для детального руководства
2. Откройте пример проекта из `examples/`
3. Экспериментируйте с виджетами
4. Сохраните свой первый проект
5. Изучите горячие клавиши для быстрой работы

---

**Версия:** 1.0.0  
**Дата:** Декабрь 2025  
**Лицензия:** MIT
