# 🔍 Полное руководство по отладке пустого экрана

## Шаг 1: Откройте консоль браузера

1. Нажмите **F12** (или **Ctrl+Shift+I** / **Cmd+Option+I** на Mac)
2. Перейдите на вкладку **Console**
3. Перезагрузите страницу (**Ctrl+R** / **Cmd+R**)
4. Посмотрите на сообщения об ошибках (красный текст)

## Шаг 2: Определите тип ошибки

### Ошибка A: "Cannot find module" или "Failed to resolve"

**Причина:** Не установлены зависимости

**Решение:**
```bash
# Убедитесь что вы в правильной директории
cd kiosk-content-platform

# Удалите старые зависимости
rm -rf node_modules package-lock.json

# Переустановите
npm install

# Запустите снова
npm run dev:editor
```

### Ошибка B: "Uncaught SyntaxError" или "Unexpected token"

**Причина:** Проблема с TypeScript или импортами

**Решение:**
```bash
# Проверьте версию Node.js
node --version  # Должно быть v18.0.0 или выше

# Если версия старая - обновите Node.js
# Затем переустановите зависимости
npm install
```

### Ошибка C: "React is not defined" или "Cannot read property 'ComponentType'"

**Причина:** Отсутствует импорт React

**Решение:** Уже исправлено в v1.0.2! Скачайте новый архив.

Или исправьте вручную:
```typescript
// В файле packages/editor/src/types/index.ts
// Первая строка должна быть:
import React from 'react';
```

### Ошибка D: Пустая консоль, нет ошибок

**Причина:** JavaScript не выполняется или блокируется

**Решение:**
```bash
# 1. Очистите кэш браузера
# Chrome: Ctrl+Shift+Delete → Очистить данные
# Firefox: Ctrl+Shift+Delete → Очистить сейчас

# 2. Попробуйте режим инкогнито
# Chrome: Ctrl+Shift+N
# Firefox: Ctrl+Shift+P

# 3. Попробуйте другой браузер
```

## Шаг 3: Тестовый режим

Откройте упрощенную версию редактора:

```
http://localhost:3000/?simple=true
```

Если эта версия работает, значит проблема в сложных компонентах (Konva, Zustand).

### Если простая версия работает:

**Проблема:** Не установлены или конфликтуют зависимости

**Решение:**
```bash
cd packages/editor

# Установите зависимости отдельно
npm install react react-dom
npm install konva react-konva
npm install zustand
npm install lucide-react
```

## Шаг 4: Проверка файловой структуры

Убедитесь что есть все файлы:

```bash
# Из корня проекта
ls -la packages/editor/src/components/

# Должны быть файлы:
# - Editor.tsx
# - Editor.css
# - Toolbar.tsx
# - Canvas.tsx
# - WidgetLibrary.tsx
# - PropertiesPanel.tsx
# - ErrorBoundary.tsx
# - EditorSimple.tsx
```

## Шаг 5: Проверка версий

```bash
# Node.js
node --version
# Ожидаем: v18.0.0 или выше

# npm
npm --version  
# Ожидаем: 9.0.0 или выше

# Проверка зависимостей
npm list react react-dom
```

## Шаг 6: Альтернативный запуск

Попробуйте запустить без StrictMode:

1. Откройте `packages/editor/src/main.tsx`
2. Измените:

```typescript
// Было:
ReactDOM.createRoot(root).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);

// Стало:
ReactDOM.createRoot(root).render(<App />);
```

3. Сохраните и перезапустите

## Шаг 7: Проверка портов

```bash
# Проверьте что порт 3000 свободен
# Windows:
netstat -ano | findstr :3000

# Linux/Mac:
lsof -i :3000

# Если занят - измените порт в vite.config.ts
```

## Шаг 8: Логирование

Добавьте логи для отладки в `packages/editor/src/main.tsx`:

```typescript
console.log('1. Script started');

import React from 'react';
console.log('2. React imported:', React);

import ReactDOM from 'react-dom/client';
console.log('3. ReactDOM imported:', ReactDOM);

import App from './App';
console.log('4. App imported:', App);

try {
  console.log('5. Getting root element...');
  const root = document.getElementById('root');
  console.log('6. Root element:', root);
  
  if (!root) {
    throw new Error('Root element not found');
  }

  console.log('7. Creating React root...');
  const reactRoot = ReactDOM.createRoot(root);
  console.log('8. React root created:', reactRoot);

  console.log('9. Rendering app...');
  reactRoot.render(<App />);
  console.log('10. App rendered successfully!');
} catch (error) {
  console.error('ERROR:', error);
}
```

## Шаг 9: Полная переустановка

Если ничего не помогает:

```bash
# 1. Удалите проект полностью
cd ..
rm -rf kiosk-content-platform

# 2. Скачайте заново
# Распакуйте свежий архив

# 3. Установите
cd kiosk-content-platform
npm install

# 4. Проверьте что установилось
npm list --depth=0

# 5. Запустите
npm run dev:editor
```

## Шаг 10: Проверка прав доступа

**Linux/Mac:**
```bash
# Проверьте права на файлы
ls -la packages/editor/

# Если нужно - исправьте владельца
sudo chown -R $USER:$USER kiosk-content-platform/

# Проверьте права npm
ls -la ~/.npm/
sudo chown -R $USER:$USER ~/.npm/
```

**Windows:**
```powershell
# Запустите PowerShell от администратора
# Затем выполните команды установки
```

## Диагностическая таблица

| Симптом | Причина | Решение |
|---------|---------|---------|
| Белый экран, консоль пустая | JS не выполняется | Очистить кэш, другой браузер |
| Красные ошибки о модулях | Нет зависимостей | `npm install` |
| "React is not defined" | Нет импорта | Добавить `import React` |
| Ошибки TypeScript | Старый Node.js | Обновить до v18+ |
| "Port in use" | Порт занят | Изменить порт в config |
| Долгая загрузка | Медленный интернет | Подождать или использовать другое зеркало npm |

## Получение помощи

Если проблема не решается:

1. **Скопируйте полный вывод консоли** (F12 → Console → правый клик → Save as)
2. **Скопируйте вывод терминала** при запуске `npm run dev:editor`
3. **Укажите:**
   - ОС и версию
   - Версию Node.js (`node --version`)
   - Версию npm (`npm --version`)
   - Браузер и версию

## Быстрая проверка работоспособности

Создайте файл `test.html` в корне проекта:

```html
<!DOCTYPE html>
<html>
<head>
    <title>Test</title>
</head>
<body>
    <div id="root"></div>
    <script type="module">
        import React from 'https://esm.sh/react@18';
        import ReactDOM from 'https://esm.sh/react-dom@18/client';
        
        const App = () => React.createElement('h1', {}, 'React works!');
        
        ReactDOM.createRoot(document.getElementById('root'))
            .render(React.createElement(App));
    </script>
</body>
</html>
```

Откройте в браузере. Если видите "React works!" - значит React работает, проблема в проекте.

---

**Версия:** v1.0.2  
**Дата:** Декабрь 2025
