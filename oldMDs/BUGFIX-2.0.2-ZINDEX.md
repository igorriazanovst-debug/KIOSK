# 🐛 BUGFIX v2.0.2: Дублирование размеров и отрицательный z-index

## ✅ Исправленные проблемы

### 1. Дублирование width/height в properties ✅

**Проблема:**
```json
{
  "width": 1880,
  "height": 1040,
  "properties": {
    "width": 200,    ← Дубликат (не должно быть)
    "height": 200,   ← Дубликат (не должно быть)
    "fillColor": "#e2ab4b"
  }
}
```

**Исправление:**
- `editorStore.ts` → Очистка properties при создании виджета
- `WidgetLibrary.tsx` → Удалены width/height из defaultProps

**Результат:**
```json
{
  "width": 1880,
  "height": 1040,
  "properties": {
    "fillColor": "#e2ab4b",
    "strokeColor": "#2c3e50",
    "opacity": 1
  }
}
```

---

### 2. Отрицательный z-index ✅

**Проблема:**
```json
{
  "zIndex": -3  ← Виджет уходит под canvas background!
}
```

**Исправления:**

#### A. Создание виджета
**Файл:** `editorStore.ts` → `addWidgetAtPosition()`
```typescript
const widget = {
  ...
  zIndex: 0,  // По умолчанию 0
  ...
};
```

#### B. Ручное редактирование
**Файл:** `PropertiesPanel.tsx` → Z-Index input
```tsx
<input
  type="number"
  min="0"  // Минимальное значение 0
  value={selectedWidget.zIndex || 0}
  onChange={(e) => handlePropertyChange('zIndex', Math.max(0, parseInt(e.target.value) || 0))}
/>
```

#### C. Кнопка "На задний план"
**Файл:** `editorStore.ts` → `sendToBack()`

**Было:**
```typescript
const minZIndex = Math.min(...project.widgets.map(w => w.zIndex || 0), 0);
get().updateWidget(id, { zIndex: minZIndex - 1 }); // ← Может быть -1, -2, -3...
```

**Стало:**
```typescript
// Устанавливаем виджет в 0 (самый задний план)
get().updateWidget(id, { zIndex: 0 });

// Поднимаем все остальные на 1
const updatedWidgets = project.widgets.map(w => 
  w.id === id ? { ...w, zIndex: 0 } : { ...w, zIndex: (w.zIndex || 0) + 1 }
);
```

#### D. Кнопка "Назад"
**Файл:** `editorStore.ts` → `sendBackward()`

**Было:**
```typescript
get().updateWidget(id, { zIndex: currentZ - 1 }); // ← Может стать отрицательным
```

**Стало:**
```typescript
get().updateWidget(id, { zIndex: Math.max(0, currentZ - 1) }); // ← Минимум 0
```

---

## 📋 Изменённые файлы

1. **packages/editor/src/stores/editorStore.ts**
   - `addWidgetAtPosition()` - добавлен zIndex: 0
   - `sendToBack()` - переписана логика без отрицательных значений
   - `sendBackward()` - добавлен Math.max(0, ...)

2. **packages/editor/src/components/PropertiesPanel.tsx**
   - Z-Index input - добавлен min="0"
   - onChange - добавлен Math.max(0, ...)

3. **packages/editor/src/components/WidgetLibrary.tsx**
   - Удалены width/height из defaultProps (уже в v2.0.1)

---

## 🧪 Тестирование

### Тест 1: Создание виджета
```
1. Добавьте Shape виджет
2. Проверьте JSON:
   ✅ zIndex должен быть 0
   ✅ properties НЕ должен содержать width/height
```

### Тест 2: Ручное изменение z-index
```
1. Выделите виджет
2. Попробуйте ввести -5 в Z-Index
   ✅ Значение должно остаться 0
```

### Тест 3: Кнопка "На задний план"
```
1. Создайте 3 виджета (z-index: 0, 1, 2)
2. Выберите верхний (z-index: 2)
3. Нажмите "На задний план" (⇊)
   ✅ Виджет: zIndex = 0
   ✅ Другие: zIndex увеличился на 1
```

### Тест 4: Кнопка "Назад"
```
1. Создайте виджет с zIndex = 0
2. Нажмите "Назад" (↓)
   ✅ zIndex должен остаться 0 (не стать -1)
```

### Тест 5: Preview/Player
```
1. Создайте Shape фон с zIndex = 0
2. Добавьте текст с zIndex = 1
3. Откройте Preview
   ✅ Shape видимый на заднем плане
   ✅ Текст поверх Shape
```

---

## 🔄 Миграция старых проектов

Если у вас есть проекты с отрицательным z-index:

### Автоматическое исправление

Откройте проект в редакторе v2.0.2:
```
1. Загрузите старый проект
2. Сохраните снова
   → z-index автоматически исправится при следующем редактировании
```

### Ручное исправление JSON

Найдите виджеты с отрицательным z-index:
```json
// БЫЛО
"zIndex": -3

// СТАЛО
"zIndex": 0
```

Также удалите width/height из properties:
```json
// БЫЛО
"properties": {
  "width": 200,
  "height": 200,
  "fillColor": "#e2ab4b"
}

// СТАЛО
"properties": {
  "fillColor": "#e2ab4b",
  "strokeColor": "#2c3e50",
  "opacity": 1
}
```

---

## 📊 Правильная структура виджета

### ✅ Правильно
```json
{
  "id": "widget-123",
  "type": "shape",
  "x": 0,
  "y": 0,
  "width": 1920,      ← На уровне виджета
  "height": 1080,     ← На уровне виджета
  "zIndex": 0,        ← Неотрицательное число
  "rotation": 0,
  "locked": false,
  "properties": {     ← Только визуальные свойства
    "shapeType": "rectangle",
    "fillColor": "#e2ab4b",
    "strokeColor": "#2c3e50",
    "strokeWidth": 0,
    "cornerRadius": 0,
    "opacity": 1
  }
}
```

### ❌ Неправильно
```json
{
  "width": 1880,
  "height": 1040,
  "zIndex": -3,       ← ПЛОХО: Отрицательный z-index
  "properties": {
    "width": 200,     ← ПЛОХО: Дубликат
    "height": 200,    ← ПЛОХО: Дубликат
    "fillColor": "#e2ab4b"
  }
}
```

---

## 🎯 Логика z-index

### Новая логика (v2.0.2):

```
z-index: 0  ← Самый задний план (фон)
z-index: 1  ← Второй слой
z-index: 2  ← Третий слой
...
z-index: N  ← Самый передний план
```

### Кнопки управления:

| Кнопка | Действие | Результат |
|--------|----------|-----------|
| ⇈ На передний план | zIndex = max + 1 | Виджет становится самым верхним |
| ↑ Вперёд | zIndex += 1 | Виджет поднимается на 1 уровень |
| ↓ Назад | zIndex = max(0, zIndex - 1) | Опускается, но не ниже 0 |
| ⇊ На задний план | zIndex = 0, остальные +1 | Виджет уходит в самый низ |

---

## 🔍 Отладка

### Проверка z-index в консоли

Откройте Preview → F12 → Console:
```javascript
// Получить все виджеты
document.querySelectorAll('[style*="position: absolute"]')

// Проверить z-index
document.querySelectorAll('[style*="position: absolute"]').forEach(el => {
  console.log(el.style.zIndex);
});

// Должно вывести только неотрицательные числа
```

---

## ✅ Чеклист обновления

- [ ] Скачать v2.0.2
- [ ] Открыть старые проекты
- [ ] Проверить что виджеты видны в Preview
- [ ] Пересохранить проекты
- [ ] Проверить JSON: нет отрицательных z-index
- [ ] Проверить JSON: нет width/height в properties
- [ ] Создать тестовый проект с новым редактором
- [ ] Убедиться что кнопки z-index работают правильно

---

## 📝 Changelog

### v2.0.2 (текущая)
- ✅ Исправлено дублирование width/height в properties
- ✅ Запрещены отрицательные значения z-index
- ✅ Переписана логика sendToBack() без отрицательных значений
- ✅ Добавлен min="0" в Z-Index input
- ✅ Добавлен Math.max(0, ...) в sendBackward()
- ✅ Добавлен zIndex: 0 по умолчанию при создании виджета

### v2.0.1
- Исправление дублирования размеров (частично)
- Добавлена документация

### v2.0.0
- Kiosk Player
- Генератор установщика Windows

---

**Версия:** 2.0.2  
**Дата:** Декабрь 2025  
**Статус:** ✅ Исправлено

🎉 **Теперь все виджеты всегда видны в Preview и Player!**
