# 🐛 BUGFIX v2.0.3: Привязка к сетке во время перетаскивания

## ❌ Проблема

**Симптомы:**
- При первом размещении виджета на холсте → привязка к сетке работает ✅
- При перетаскивании виджета → привязка НЕ работает ❌
- Нужно "отпустить и снова потянуть" чтобы привязка сработала

**Причина:**
Функция `snapToGridValue` применялась только в `handleDragEnd` (после завершения перетаскивания), но не **во время** перетаскивания.

---

## ✅ Решение

Добавлена функция `dragBoundFunc` которая применяет привязку к сетке **в реальном времени** во время перетаскивания.

### Как это работает:

```
БЕЗ dragBoundFunc:
Начало перетаскивания → [свободное движение] → Отпустил → snapToGrid

С dragBoundFunc:
Начало перетаскивания → [привязка к сетке в реальном времени] → Отпустил
```

---

## 🔧 Изменённые файлы

### 1. Canvas.tsx

**Добавлена функция:**
```typescript
// Функция для привязки во время перетаскивания
const dragBoundFunc = (pos: { x: number; y: number }) => {
  return {
    x: snapToGridValue(pos.x),
    y: snapToGridValue(pos.y)
  };
};
```

**Передаётся во все виджеты:**
```tsx
<ShapeWidget
  widget={widget}
  isSelected={selectedWidgetIds.includes(widget.id)}
  onSelect={(e) => handleWidgetClick(widget.id, e)}
  onDragEnd={(e) => handleDragEnd(widget.id, e)}
  onTransformEnd={(e) => handleTransformEnd(widget.id, e)}
  dragBoundFunc={snapToGrid ? dragBoundFunc : undefined}  // ← ДОБАВЛЕНО
/>
```

### 2. Все компоненты виджетов

Обновлены **6 файлов:**
- ShapeWidget.tsx
- ButtonWidget.tsx
- TextWidget.tsx
- ImageWidget.tsx
- VideoWidget.tsx
- MenuWidget.tsx

**Изменения в каждом:**

```typescript
// Интерфейс Props
interface XxxxxWidgetProps {
  widget: Widget;
  isSelected: boolean;
  onSelect: (e: any) => void;
  onDragEnd: (e: any) => void;
  onTransformEnd: (e: any) => void;
  dragBoundFunc?: (pos: { x: number; y: number }) => { x: number; y: number };  // ← ДОБАВЛЕНО
}

// Деструктуризация параметров
const XxxxxWidget: React.FC<XxxxxWidgetProps> = ({
  widget,
  isSelected,
  onSelect,
  onDragEnd,
  onTransformEnd,
  dragBoundFunc  // ← ДОБАВЛЕНО
}) => {
  // ...
  
  return (
    <Group
      id={widget.id}
      x={widget.x}
      y={widget.y}
      draggable={!isLocked}
      dragBoundFunc={dragBoundFunc}  // ← ДОБАВЛЕНО
      onDragEnd={onDragEnd}
      // ...
    >
      {/* content */}
    </Group>
  );
};
```

---

## 🧪 Тестирование

### Тест 1: Привязка к сетке включена

```
1. Toolbar → включите "Сетка"
2. Toolbar → включите "Привязка к сетке"
3. Добавьте Shape виджет
4. Начните перетаскивать виджет

✅ Ожидаемое поведение:
   - Виджет движется "рывками" по клеткам сетки
   - Не может остановиться между клетками
   - Всегда выравнивается по сетке
```

### Тест 2: Привязка к сетке выключена

```
1. Toolbar → выключите "Привязка к сетке"
2. Перетаскивайте виджет

✅ Ожидаемое поведение:
   - Виджет движется плавно
   - Может остановиться где угодно
   - Свободное позиционирование
```

### Тест 3: Все типы виджетов

Проверьте что привязка работает для:
- ✅ Shape (фигуры)
- ✅ Button (кнопки)
- ✅ Text (текст)
- ✅ Image (изображения)
- ✅ Video (видео)
- ✅ Menu (меню)

### Тест 4: Размер сетки

```
1. Измените размер сетки (Settings → Grid Size: 10, 20, 50)
2. Перетаскивайте виджет

✅ Ожидаемое поведение:
   - Привязка работает с новым размером сетки
   - Шаг движения соответствует размеру клетки
```

---

## 📊 До и После

### До (v2.0.2):
```
Перетаскивание:
├── Начало: x=153, y=267
├── Во время: x=165, y=281  ← Произвольные координаты
├── Отпустил: x=177, y=295
└── После обработки: x=180, y=300  ← Привязка только здесь
```

### После (v2.0.3):
```
Перетаскивание (с сеткой 20px):
├── Начало: x=160, y=280  ← Сразу на сетке
├── Во время: x=180, y=300  ← Только позиции сетки
├── Отпустил: x=200, y=320  ← Уже на сетке
└── Дополнительная обработка не нужна
```

---

## 🎯 Логика работы

### Когда dragBoundFunc активен:

```typescript
if (snapToGrid) {
  // dragBoundFunc применяется ВСЕГДА во время движения
  dragBoundFunc: (pos) => {
    x: Math.round(pos.x / gridSize) * gridSize,
    y: Math.round(pos.y / gridSize) * gridSize
  }
} else {
  // Свободное движение
  dragBoundFunc: undefined
}
```

### Примеры с gridSize = 20:

| Позиция мыши | Без привязки | С привязкой |
|--------------|--------------|-------------|
| x: 15, y: 23 | 15, 23 | 20, 20 |
| x: 37, y: 51 | 37, 51 | 40, 60 |
| x: 99, y: 185 | 99, 185 | 100, 180 |
| x: 153, y: 267 | 153, 267 | 160, 260 |

---

## 🔍 Отладка

### Проверка в консоли браузера:

Откройте редактор → F12 → Console:

```javascript
// Получите stage
const stage = document.querySelector('canvas').getContext('2d').canvas;

// Перетаскивайте виджет и смотрите координаты
// С привязкой они будут кратны gridSize
```

---

## 💡 Преимущества

✅ **Точность:** Виджеты всегда выравниваются по сетке  
✅ **Визуальная обратная связь:** Виджет "прыгает" по клеткам во время перетаскивания  
✅ **Улучшенный UX:** Не нужно "дважды двигать" виджет  
✅ **Консистентность:** Работает для всех типов виджетов одинаково  

---

## 📝 Технические детали

### Konva dragBoundFunc

`dragBoundFunc` - это встроенная возможность Konva.js:
- Вызывается на каждый кадр во время перетаскивания
- Получает `pos: { x: number; y: number }` - текущую позицию
- Возвращает `{ x: number; y: number }` - скорректированную позицию
- Если возвращает `undefined` - позиция не изменяется

### Производительность

- ⚡ Минимальное влияние на производительность
- 🎯 Вычисления простые: `Math.round(value / gridSize) * gridSize`
- 🚀 Выполняется только если `snapToGrid === true`

---

## ✅ Чеклист обновления

- [ ] Скачать v2.0.3
- [ ] Открыть редактор
- [ ] Включить сетку
- [ ] Включить привязку к сетке
- [ ] Добавить виджет
- [ ] Перетащить виджет
- [ ] Проверить что виджет "прыгает" по клеткам
- [ ] Выключить привязку
- [ ] Проверить что движение плавное
- [ ] Протестировать все типы виджетов

---

## 🐛 Известные ограничения

**Нет:** Все работает как задумано! 🎉

---

## 📚 Связанные функции

- `snapToGridValue()` - округление значения до ближайшей клетки сетки
- `handleDragEnd()` - финальная обработка после перетаскивания
- `addWidgetAtPosition()` - использует привязку при первом размещении

---

## 📊 Changelog

### v2.0.3 (текущая)
- ✅ Добавлена функция `dragBoundFunc` в Canvas.tsx
- ✅ Добавлен параметр `dragBoundFunc` во все виджеты
- ✅ Привязка к сетке теперь работает в реальном времени
- ✅ Улучшен UX перетаскивания виджетов

### v2.0.2
- Исправлены отрицательные z-index

### v2.0.1
- Исправлено дублирование размеров

### v2.0.0
- Kiosk Player и установщик Windows

---

**Версия:** 2.0.3  
**Дата:** Декабрь 2025  
**Статус:** ✅ Исправлено

🎉 **Теперь привязка к сетке работает идеально!**
