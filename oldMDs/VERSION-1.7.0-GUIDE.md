# 👁️ Версия 1.7.0 - Режим превью

## 🎉 Что нового

Полноценный режим предварительного просмотра:

- ✅ **Режим превью** - просмотр проекта в действии
- ✅ **Интерактивность** - все виджеты работают
- ✅ **Полноэкранный режим** - просмотр без интерфейса
- ✅ **Горячие клавиши** - быстрый доступ
- ✅ **HTML рендеринг** - нативные элементы вместо Canvas

---

## 🖥️ Что такое превью

### Описание

Режим превью позволяет увидеть, как будет выглядеть и работать ваш киоск в реальности, без редактирования.

### Отличия от редактора

| Редактор | Превью |
|----------|--------|
| Canvas (Konva) | HTML/CSS |
| Статичные виджеты | Интерактивные элементы |
| Рамки выделения | Чистый вид |
| Инструменты редактирования | Только просмотр |
| Трансформеры | Нет |

---

## 🚀 Как открыть превью

### Способ 1: Кнопка в Toolbar

```
1. Найдите кнопку "▶ Превью" в правой части Toolbar
2. Кликните на неё
3. ✅ Превью открылось
```

### Способ 2: Горячая клавиша (будущее)

```
F5 → открыть превью
```

---

## 🎨 Интерфейс превью

### Шапка

**Элементы:**
- Название проекта
- Разрешение холста (например, "1920 × 1080")
- Кнопка полноэкранного режима (⛶)
- Кнопка закрытия (×)

### Холст

**Особенности:**
- Все виджеты отображаются в реальном масштабе
- Фоновый цвет проекта
- Тень вокруг холста для лучшей видимости
- Центрирование в окне
- Прокрутка если холст больше окна

### Подвал

**Подсказки:**
- "ESC - выход"
- "F11 или кнопка - полный экран"

---

## 🎮 Режимы превью

### 1. Оконный режим (по умолчанию)

**Внешний вид:**
```
┌─────────────────────────────────────┐
│ Превью: Мой проект    1920 × 1080  │ ← Шапка
├─────────────────────────────────────┤
│                                     │
│         ┌───────────────┐          │
│         │               │          │
│         │    ХОЛСТ      │          │ ← Холст с тенью
│         │               │          │
│         └───────────────┘          │
│                                     │
├─────────────────────────────────────┤
│  ESC - выход   F11 - полный экран  │ ← Подвал
└─────────────────────────────────────┘
```

**Применение:**
- Быстрый просмотр
- Сравнение с редактором
- Проверка деталей

### 2. Полноэкранный режим

**Внешний вид:**
```
┌─────────────────────────────────────┐
│                                     │
│                                     │
│         ХОЛСТ НА ВЕСЬ ЭКРАН         │
│                                     │
│                                     │
└─────────────────────────────────────┘
```

**Применение:**
- Презентация проекта
- Реальный размер на целевом устройстве
- Финальная проверка

**Как включить:**
- Кнопка ⛶ в шапке
- ESC для выхода в оконный режим

---

## 🎯 Интерактивность виджетов

### Текст
- ✅ Отображается со всеми стилями
- ✅ Прокрутка длинного текста
- ✅ Переносы слов

### Кнопка
- ✅ Hover эффекты
- ✅ Клик анимация (scale 0.95)
- ✅ Действия при клике
  - **Popup** → alert с содержимым
  - **URL** → (планируется)
  - **Widget control** → (планируется)

### Фигура
- ✅ Все формы отображаются
- ✅ Цвета, рамки, прозрачность
- ⚠️ Сложные фигуры (звезда, стрелка) упрощены

### Изображение
- ✅ Загруженные изображения
- ✅ Все режимы заполнения
- ✅ Рамки

### Видео
- ✅ Воспроизведение
- ✅ Элементы управления
- ✅ Autoplay, loop, muted
- ⚠️ RTSP не поддерживается в браузере

### Меню
- ✅ Горизонтальное/вертикальное
- ✅ Hover эффекты
- ✅ Раскрытие подменю по клику
- ✅ Стили основных и подпунктов

---

## ⌨️ Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| **ESC** | Закрыть превью (из оконного режима) |
| **ESC** | Выйти из полноэкранного → оконный |
| **ESC × 2** | Полностью закрыть превью |

---

## 🔧 Технические детали

### Рендеринг

**Canvas (редактор):**
```
Konva.js → <canvas> → рисование фигур
```

**Preview (превью):**
```
React → HTML/CSS → нативные элементы
```

### Преимущества HTML рендеринга

**Производительность:**
- Нативные элементы быстрее
- Аппаратное ускорение CSS
- Меньше нагрузка на CPU

**Функциональность:**
- Реальные кнопки с событиями
- Видео с встроенными контролами
- Текст с прокруткой
- Формы и инпуты (будущее)

**Точность:**
- То, что видите = то, что получите
- Идентично финальному runtime

### Z-Index в превью

```typescript
// Сортировка виджетов по zIndex
project.widgets
  .slice()
  .sort((a, b) => (a.zIndex || 0) - (b.zIndex || 0))
  .map(widget => renderWidget(widget))
```

Порядок отображения такой же, как в редакторе.

---

## 💡 Примеры использования

### Пример 1: Проверка меню

**Задача:** Убедиться что подменю раскрывается

```
1. Создайте меню с подпунктами
2. Откройте превью
3. Наведите курсор на пункт
4. ✅ Hover эффект работает
5. Кликните на пункт с подменю
6. ✅ Подменю раскрылось
```

### Пример 2: Тест кнопок

**Задача:** Проверить действие кнопки

```
1. Создайте кнопку
2. Действие: Popup
3. Текст: "Привет из превью!"
4. Откройте превью
5. Кликните кнопку
6. ✅ Alert появился
```

### Пример 3: Проверка видео

**Задача:** Убедиться что видео воспроизводится

```
1. Добавьте видео виджет
2. Загрузите видео
3. Autoplay: ✓
4. Откройте превью
5. ✅ Видео играет автоматически
```

### Пример 4: Презентация клиенту

**Задача:** Показать проект в полном экране

```
1. Завершите дизайн
2. Откройте превью
3. Нажмите кнопку ⛶ (полный экран)
4. ✅ Проект на весь экран
5. Презентуйте клиенту
```

### Пример 5: Проверка на разных разрешениях

**Задача:** Посмотреть как выглядит на другом размере

```
1. Настройки холста → iPad (2048×1536)
2. Разместите виджеты
3. Откройте превью
4. ✅ Видно как будет на iPad
5. Вернитесь в редактор
6. Измените размер → Full HD
7. Превью снова
8. ✅ Сравните варианты
```

---

## 📊 Сравнение: Редактор vs Превью

| Аспект | Редактор | Превью |
|--------|----------|--------|
| **Рендеринг** | Canvas (Konva) | HTML/CSS |
| **Интерактивность** | Редактирование | Просмотр + действия |
| **Производительность** | Средняя | Высокая |
| **Точность** | Приблизительная | Финальная |
| **Меню** | Статичное | Hover + клики |
| **Кнопки** | Статичные | Hover + действия |
| **Видео** | Превью кадр | Воспроизведение |
| **Текст** | Konva Text | HTML с прокруткой |

---

## ⚠️ Ограничения

### Текущие ограничения

**Фигуры:**
- Звезда, стрелка, многоугольники → прямоугольники
- Решение: использовать SVG (планируется)

**RTSP видео:**
- Не поддерживается браузерами
- Решение: серверная конвертация в HLS

**Действия кнопок:**
- Только popup реализован
- URL и Widget control → в разработке

**Адаптивность:**
- Большие холсты требуют прокрутки
- Решение: масштабирование (планируется)

### Известные проблемы

**Safari:**
- Некоторые CSS трансформации могут отличаться
- Тестируйте в целевом браузере

**Старые браузеры:**
- IE11 не поддерживается
- Требуется Chrome 90+, Firefox 88+, Safari 14+

---

## 🧪 Чек-лист тестирования

Перед публикацией проекта:

- [ ] Все виджеты видны
- [ ] Z-index правильный (порядок слоёв)
- [ ] Меню раскрывается
- [ ] Кнопки кликаются
- [ ] Видео воспроизводится
- [ ] Текст читаемый
- [ ] Цвета соответствуют дизайну
- [ ] Шрифты загружены
- [ ] Полноэкранный режим работает
- [ ] На целевом разрешении всё помещается

---

## 📥 Обновление

- [⬇️ kiosk-content-platform.zip](computer:///mnt/user-data/outputs/kiosk-content-platform.zip) (73 KB) - для Windows
- [⬇️ kiosk-content-platform.tar.gz](computer:///mnt/user-data/outputs/kiosk-content-platform.tar.gz) (48 KB) - для Linux/macOS

---

## 🚀 Что дальше

**Готово в v1.7.0:**
- ✅ Режим превью
- ✅ HTML рендеринг
- ✅ Интерактивные элементы
- ✅ Полноэкранный режим
- ✅ Горячие клавиши

**Планируется:**
- Масштабирование холста (fit to screen)
- Экспорт в HTML
- Standalone runtime
- Действия кнопок (URL, widget control)
- Адаптивные режимы
- Инспектор элементов в превью
- Запись видео превью

---

**Версия:** 1.7.0  
**Дата:** Декабрь 2025  
**Статус:** ✅ Режим превью работает
