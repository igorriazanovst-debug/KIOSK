# Перечень сторонних компонентов и их лицензий: виджет «ФизАстроIQ» (Тип 11)

**Документ требуется ТЗ, раздел 10:** «Разработчик должен обеспечить законность использования всех поставляемых изображений, аудио, видео, текстов, шрифтов и иных материалов» (строка 184 подлинника).

**Состояние на 18.09.2026.**

**Короткий ответ по разделу 10.** В поставке виджета нет ни одного заимствованного изображения, аудиофайла, видеофайла и шрифта. Всё, что видно на экране, либо начерчено скриптами этого проекта, либо взято из системного шрифта устройства.

---

## 1. Программные компоненты

Виджет не добавляет ни одной новой зависимости сверх тех, что уже есть у плеера KIOSK.

### Рантайм-зависимости плеера (едут в дистрибутив)

| Компонент | Версия | Лицензия | Используется виджетом |
|---|---|---|---|
| react | 18.3.1 | MIT | да — весь интерфейс |
| react-dom | 18.3.1 | MIT | да |
| @kiosk/shared | 1.0.0 | MIT | да — свойства виджета (внутренний пакет проекта) |
| zod | 4.6.1 | MIT | да — схемы викторины и пользовательских данных |
| konva | 9.3.22 | MIT | да — холст редактора точек (в игровом экране не используется) |
| react-konva | 18.2.16 | MIT | да — см. konva |

### Оболочка

| Компонент | Версия | Лицензия |
|---|---|---|
| Electron | 39.8.10 | MIT |
| Chromium | в составе Electron | BSD-3-Clause и др. (см. `chrome://credits` в сборке) |
| Node.js | в составе Electron | MIT |

### Инструменты разработки (в дистрибутив НЕ едут)

| Компонент | Назначение | Лицензия |
|---|---|---|
| vite 7.3.6 | сборка | MIT |
| typescript 5.9.3 | компиляция | Apache-2.0 |
| electron-builder 24.13.3 | упаковка | MIT |

Все перечисленные лицензии — разрешительные и совместимы с коммерческим распространением. Требований раскрывать исходный код продукта ни одна из них не предъявляет.

---

## 2. Материалы контента

| Что | Сколько | Происхождение | Правовой статус |
|---|--:|---|---|
| Карты уровней | 6 | Начерчены `tools/physastroiq/draw-levels.mjs` (описания карт — `levels-physics.mjs`, `levels-astro.mjs`), PNG сделан `render-thematic.cjs` | Собственная разработка |
| Тематические изображения | 14 | Начерчены `tools/physastroiq/draw-thematic.mjs`, тем же способом | Собственная разработка |
| Банк вопросов | 180 | Написан в этом проекте (`tools/physastroiq/questions-physics.mjs`, `questions-astro.mjs`) | Собственная разработка |
| Звук | 0 файлов | Виджет беззвучен, кроме двух сигналов «верно»/«неверно» | Сигналы генерируются Web Audio на лету, файлов нет |
| Шрифты | 0 | Интерфейс использует системный шрифт устройства | Заимствований нет |
| Видео | 0 | — | — |

### 2.1. Изображения начерчены скриптом, а не нарисованы и не взяты готовыми

Каждая из двадцати картинок (6 карт уровней и 14 тематических) описана кодом: набором примитивов из `tools/physastroiq/shapes.mjs` и координат. Правка описания перерисовывает картинку заново.

Отсюда три следствия, важных именно для раздела 10:

1. **происхождение прослеживается до строки кода.** Не нужно искать, кто и где взял исходник, — исходника нет, есть программа;
2. **вопрос об авторских правах не возникает.** Схема, построенная из окружностей, отрезков и многоугольников по собственным координатам, — результат работы, а не заимствование;
3. **воспроизводимость.** Там, где на картинке нужен беспорядок (звёзды фона, частицы газа), используется генератор псевдослучайных чисел с фиксированным зерном, а не `Math.random`: перерисовка даёт тот же файл. Иначе каждая сборка меняла бы все PNG, и по разнице было бы не понять, поменялось ли что-то по существу.

### 2.2. Ни одного файла из эталонного продукта

Эталонные продукты ОС3 «Физ IQ» и «Астро IQ» разбирались **только для сверки требований**: сколько вопросов, сколько изображений, какие темы. Ни одно изображение, ни один вопрос и ни одна строка текста оттуда в поставку не попали.

Разбор эталона и снимки его экранов лежат в рабочих материалах (`Рабочие файлы/Тип11_ФизАстроIQ/`) и **в дистрибутив не входят**.

### 2.3. Наследие «БиоIQ» и «ХимIQ» удалено

Виджет получен копированием «БиоIQ» (Тип 10), который сам был копией «ХимIQ» (Тип 9). Биологические и химические материалы удалены, и это проверяется тестами, а не просмотром:

- `thematicImages.test.ts` — в описаниях изображений нет слов «биолог», «клетк», «органелл», «растен», «хими», «менделеев», «валентност»; в каталоге нет файлов с именами `cell_*`, `plant_*`, `organs_*`, и нет картинок, которых нет в списке;
- `physastroiqBuiltinContent.test.ts` — в текстах 180 вопросов нет слов «менделеев», «валентн», «реактив», «клетк», «органоид», «фотосинтез»;
- `specialChars.test.ts` — в наборе спецсимволов не осталось биологических знаков ♀ ♂ ‰.

### 2.4. Тексты вопросов

180 вопросов, ответов и подсказок написаны в этом проекте по программе средней школы. Формулировки не копировались из учебников: вопрос вида «Какой прибор измеряет силу тока?» — общее место предмета, а не охраняемый текст.

---

## 3. Что требует решения заказчика

| Вопрос | Кто решает |
|---|---|
| Методическая проверка 90 вопросов по физике профильным учителем | заказчик / методист |
| Методическая проверка 90 вопросов по астрономии профильным учителем | заказчик / методист |
| Точность физических и астрономических схем с точки зрения учебной программы | то же |
| Объём банка по астрономии: 90 вопросов против 358 у эталона | заказчик, см. матрицу §4.5 |
| Отдельные картинки к каждому из 180 вопросов, если приёмка потребует буквального чтения FR-009 | заказчик, см. матрицу §4.1 |

**Отдельно про то, чего приложение проверить не может.** Виджет одинаково исправно засчитает и верный ответ, и неверный, если так написано в банке. Автоматические тесты проверяют, что у каждого вопроса есть подсказка, что подсказка не повторяет ответ, что вопрос привязан к точке внутри карты, — но **не то, что ответ правильный по существу**. Эта проверка человеческая и в объём разработки не входила.

---

## 4. Как убедиться в сказанном

```sh
# Перерисовать все изображения с нуля и сверить с поставкой
cd packages/player
node tools/physastroiq/draw-thematic.mjs
node tools/physastroiq/draw-levels.mjs all
env -u ELECTRON_RUN_AS_NODE node_modules/electron/dist/electron.exe \
  tools/physastroiq/render-thematic.cjs "$(pwd -W)/tools/physastroiq/svg" "$(pwd -W)/public/physastroiq/thematic"
env -u ELECTRON_RUN_AS_NODE node_modules/electron/dist/electron.exe \
  tools/physastroiq/render-thematic.cjs "$(pwd -W)/tools/physastroiq/levelsvg" "$(pwd -W)/public/physastroiq"
git status   # файлы не изменились — значит поставка собрана этими же скриптами
```

Лицензии npm-зависимостей:

```sh
cd packages/player && npx license-checker --production --summary
```
