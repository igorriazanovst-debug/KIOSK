# Матрица трассировки требований: виджет «АзбукоСлов» (Тип 3)

**Документ требуется ТЗ:** приёмка проводится по трассируемой матрице требований — каждое FR должно иметь не менее одного тест-кейса.

**Состояние на 13.09.2026.** Заполняется по ходу работы, а не перед сдачей. Каждая строка отвечает на три вопроса: что требуется, чем закрыто, чем это проверено. Строка считается закрытой только если способ проверки указан конкретно — «реализовано» без теста или живого прогона закрытием не считается.

**Источник требований:** `ТЗ_03_Азбуко_Слов.docx`. Колонка «ИСТ» — номер строки исходной закупочной спецификации.

## Условные обозначения

| Знак | Значение |
|:--:|---|
| ✅ | Закрыто и проверено указанным способом |
| ⚠️ | Закрыто частично: что именно осталось — в колонке «Чем проверено» |
| ❌ | Не закрыто |
| — | Закрывается не кодом виджета (платформой, договором, составом контента) |

---

## 1. Функциональные требования

| ID | ИСТ | Требование | Ст. | Чем закрыто | Чем проверено |
|---|---:|---|:--:|---|---|
| FR-002 | 65 | Начальные навыки чтения | ✅ | Три этапа: `game/letterShow.ts`, `wordCompleting.ts`, `wordMake.ts`; экран `screens/PlayScreen.tsx` | Живой прогон всех трёх этапов от первого вопроса до итогов (13.09.2026). Цифры на итогах сошлись с нажатиями: 5+1, 5+4, 12+6 ответов = 33, ровно столько записалось на диск |
| FR-003 | 66 | Предмет: развитие речи, русский язык | ⚠️ | Состав пакета `packages/alphabet-library` | 33 буквы, 95 слов, 151 слог, 3 комплекта. **Остаётся:** до требования строки 79 не хватает 65 слов |
| FR-004 | 67 | ПК/доски/столы (Win 7, 8), Android 4.2+ | ❌ | Windows — сборка плеера KIOSK; Android — отдельное нативное приложение | **Windows 10 — да.** Windows 7 и 8 недостижимы: Electron не поддерживает их с версии 23, плеер собран на Electron 39. **Android не начат.** См. «Открытые расхождения» |
| FR-005 | 68 | Изучение букв и слов в детских садах | ✅ | Словарь бытовой лексики, `tools/catalogue.mjs` | Живой прогон на словах «Ёлка», «Груша», «Ёжик» и т.п. — обиходная лексика дошкольника |
| FR-006 | 69 | Несколько учётных записей | ✅ | `electron/alphabet/profileStore.js`, `screens/ProfilesScreen.tsx` | 15 тестов хранилища; живой прогон: профиль создан, приложение остановлено и запущено заново, профиль на месте |
| FR-007 | 70 | Алфавит списком и побуквенно | ✅ | `screens/AlphabetScreen.tsx` (сетка 33 букв), `screens/LetterScreen.tsx` (перелистывание иллюстраций) | Живой прогон обоих экранов, снимки; буква без слов серая и не нажимается |
| FR-008 | 71 | ≥ 2 картинки на каждую букву | ✅ | `model/graph.ts` → `checkLetterIllustrations`; словарь даёт по 3 слова на букву | Проверка пакета: `ok: true`, **ни одной буквы на границе**. У эталона Ё и Й ровно по 2, и разбор назвал это риском приёмки |
| FR-009 | 72 | Статистика по каждому пользователю | ✅ | `game/statistics.ts`, `screens/StatisticsScreen.tsx` | 11 тестов домена; живой прогон: 14 букв с данными, 19 пустых, все 33 на экране. Буква без ответов рисуется **пустой, а не нулём** |
| FR-010 | 73 | Подстановка недостающего слога | ✅ | `game/wordCompleting.ts` | Тесты отбора и панели (16 на все три этапа); живой прогон этапа 2: неверный слог отвергается и остаётся красным, верный продвигает партию |
| FR-011 | 74 | Сборка слова из слогов | ✅ | `game/wordMake.ts` | Те же 16 тестов построения вопросов; живой прогон этапа 3: 12 ячеек за 5 вопросов, все слоги слова на панели одновременно |
| FR-012 | 75 | Стол, каждая сторона, 2–4 игрока | ⚠️ | `components/Seats.tsx`, поворот сцены в `AlphabetRuntime.tsx` | Живой прогон: на 0° сцена 1229×768, на 90° — 1024×1638, покрытие окна 1,00×1,00, за границы не выходит ничего. **Расхождение с эталоном:** у него каждая сторона получает полный игровой слот, у нас поворачивается вся сцена. См. «Открытые расхождения» |
| FR-013 | 76 | Аудио, текст, графика в редакторе | ✅ | `screens/WordEditorScreen.tsx`, `components/VoiceRecorder.tsx`, `electron/alphabet/contentStore.js` | 17 тестов хранилища, 33 теста правил; живой прогон: свой слог и своё слово созданы, готовность слова показывает, чего не хватает |
| FR-014 | 77 | Пять операций с комплектами | ✅ | `contentStore.js` (создание, правка, удаление), `setArchive.js` (импорт, экспорт) | 11 тестов обмена; живой прогон: комплект из поставочного «Арбуза» и своего слова. Проверено, что после импорта **граф на принимающем устройстве цел** и слово попадает в отбор этапа 3 |
| FR-015 | 78 | Свои слова и объединение в комплекты | ✅ | `store/contentRules.ts`, `MyContentScreen.tsx` | Живой прогон: после добавления своего слова этап 1 предлагает **96 слов вместо 95** — своё слово играется наравне с поставочными |
| FR-016 | 79 | ≥ 160 озвученных слов с картинками | ❌ | `packages/alphabet-library` | **95 слов из 160.** Озвучка есть на все 95 (372 файла), но **синтезирована**, а не записана голосом. См. «Открытые расхождения» |
| FR-017 | 80 | Встроенное автообновление | ❌ | — | **Механизма нет на платформе KIOSK в целом.** Тот же незакрытый пункт, что в Тип 2 |
| FR-018 | 81 | Бессрочная лицензия | — | — | Свойство лицензии KIOSK, указывается в договоре |

---

## 2. Требования к данным и надёжности

| Требование | Ст. | Чем закрыто | Чем проверено |
|---|:--:|---|---|
| Атомарная запись данных | ✅ | `chrono/atomicJson.js` — временный файл + rename | Наследовано от Хронолинии вместе с её тестами. **Прямое улучшение против эталона:** у него JSON переписывается целиком без отката, и обрыв оставляет повреждённый файл со статистикой всего класса |
| Повреждённый файл не трактуется как «пусто» | ✅ | `profileStore.js` | Тест: битый `profiles.json` даёт ошибку, а не пустой список. Молча обнулившийся список выглядит как потеря работы детей |
| Повреждённые настройки не запирают приложение | ✅ | `profileStore.readSettings` | Тест: битый `settings.json` даёт дефолт. Строгость обратная профилям — и это осознанно |
| Битая запись статистики не уносит остальные | ✅ | `parseStatistics` | Тест: из двух записей одна битая — вторая выживает |
| Целостность графа контента | ✅ | `model/graph.ts`, `store/contentRules.ts` | 12 тестов графа + 33 теста правил редактора. Поймало собственную ошибку сборщика: «Подъезд» не стоял у своей первой буквы П |
| Комплектность пакета контента | ✅ | `model/resources.ts` → `checkLibraryCompleteness` | Проверка на старте плеера пишет в лог; на сборке пакета — жёсткая. Сейчас: полная, 0 недостающих, 0 лишних |
| Коллизии путей в пакете | ✅ | `checkPathCollisions` | 4 теста. Ловит дефект эталонной раскладки: слова, слоги и буквы делят каталог `media/` |

---

## 3. Безопасность (ТЗ раздел 9)

| Требование | Ст. | Чем закрыто | Чем проверено |
|---|:--:|---|---|
| Входные файлы проверяются по типу/формату | ✅ | `common/mediaFiles.js` — сигнатура первых байтов, а не расширение | 20 тестов. Переименованный `.exe` с расширением `.png` отвергается |
| Пользовательские файлы не исполняются как код | ✅ | Имя файла в хранилище выводится из хеша содержимого, а не берётся от пользователя | Тесты хранилища; обход пути и коллизии имён сняты по построению |
| Импортируемый контент проверяется | ✅ | `common/zipGuard.js` + `alphabet/setArchive.js` | 11 тестов. Три рубежа: белый список ТОЧНЫХ имён (не проверка на `..`), потолки до и во время чтения, сигнатура каждого файла медиа |
| Разграничение прав педагог/ученик | ✅ | `common/teacherPassword.js`, `components/PasswordPrompt.tsx` | 7 тестов; живой прогон: неверный пароль не пускает, отмена не пускает, верный открывает. Хеш в главном процессе, наружу только «подошёл или нет» |
| Пароли виджетов не общие | ✅ | Своя соль у каждого виджета | Отдельный тест: один пароль в двух виджетах даёт разные хеши |
| Необратимое действие требует подтверждения | ✅ | `components/ConfirmPrompt.tsx` | Удаление игрока, слова, комплекта и очистка статистики. Подтверждение говорит, ЧТО именно пропадёт, а не спрашивает «вы уверены» |

---

## 4. Открытые расхождения

Перечислены отдельно, чтобы не растворялись в таблицах. Каждое требует решения человека — разработчик закрыть их не может.

### 4.1. Windows 7 и 8 (FR-004) — техническое ограничение платформы

ТЗ называет **только** «win 7,8», без десятки. Electron не поддерживает Windows 7/8 с версии 23; плеер KIOSK собран на Electron 39. Понижение версии Electron до 22 означает Chromium 108 и отказ от исправлений безопасности с конца 2022 года.

**Требуется решение:** либо согласовать Windows 10+ как целевую платформу, либо отдельная сборка на устаревшем Electron с принятием рисков безопасности.

### 4.2. Android (FR-004) — не начат

Решение от 08.09.2026: Android реализуется **отдельным нативным приложением**, а не веб-обёрткой. Совместимость обеспечивается общим контрактом форматов и правил, а не общим кодом рантайма. Работа не начата.

Контракт при этом **уже написан и машинно проверяем**: `docs/alphabet-cross-platform-contract.md` и сверочные векторы `shared/src/alphabet/conformance/vectors.json` (6 тестов). Нативная реализация прогоняет ту же таблицу своими средствами; расхождение становится падением теста, а не тихой разницей в поведении на занятии.

### 4.3. Объём контента (FR-003, FR-016) — 95 слов из 160

**Требуется решение:** дописать словарь до 160+ слов. Это работа на стороне контента, не кода: сборщик пакета, проверка комплектности и проверка графа уже готовы и поймают ошибки.

Разбор эталона рекомендует также **зафиксировать способ подсчёта**: 160 слов базового набора или 183 со всеми комплектами. У нас счёт однозначен — 95 слов в `index.json`.

### 4.4. Озвучка синтезирована, а не записана (FR-016)

372 файла сделаны Windows SAPI. ТЗ требует «озвученные слова», не уточняя способ. Пригоден ли синтезированный голос для пособия по **развитию речи** — решает заказчик или методист, а не разработчик.

До такого решения синтез здесь — **средство разработки**: он сделал игру проверяемой на слух. Схема, раскладка и имена файлов при замене на живой голос не меняются.

### 4.5. Иллюстрации (FR-016)

На 13.09.2026 генерируются нейросетью, готово 47 из 95. **Правовая сторона не закрыта:** ТЗ раздел 10 требует обеспечить законность использования всех изображений, аудио и шрифтов, а лицензионный статус изображений, созданных генеративной моделью, — вопрос к юристу заказчика, не к разработчику. Реестр источников не заведён.

### 4.6. Игра с четырёх сторон стола (FR-012)

У эталона каждая сторона получает **слот с полным игровым интерфейсом**. У нас игрок один за раз, и к нему поворачивается вся сцена.

Четыре копии поля, из которых три бездействуют, ничего не дают, пока нет одновременного мультитача, — а он вынесен планом за пределы MVP.

**Требуется решение:** засчитывается ли очерёдность с поворотом сцены по строке 75, либо нужен одновременный мультитач — тогда это отдельная работа с четырьмя независимыми игровыми слотами.

### 4.7. Автообновление (FR-017) — отсутствует на платформе

Механизма нет в KIOSK в целом, а не только в этом виджете. Закрывается платформенной работой, а не виджетом.

---

## 5. Что проверено живым прогоном

Правило сценария разработки (раздел 5): домен закрывается тестами, интерфейс — живым запуском. Ниже — что именно прогонялось на настоящем Electron, а не в тестовой среде.

| Дата | Что проверялось | Результат |
|---|---|---|
| 13.09.2026 | Профиль переживает перезапуск приложения | ✅ создан, приложение остановлено и запущено заново, профиль на месте |
| 13.09.2026 | Три этапа от первого вопроса до итогов | ✅ цифры итогов сошлись с нажатиями, 33 ответа записались на диск |
| 13.09.2026 | Неверный ответ отвергается и остаётся видимым | ✅ на всех трёх этапах |
| 13.09.2026 | Прерванная партия сохраняет уже данные ответы | ✅ два ответа записались |
| 13.09.2026 | Озвучка доходит из пакета в игру | ✅ `readyState: 4`, реальные длительности; этап 2 играет `_bgn`, а не слово целиком |
| 13.09.2026 | Пароль на закрытых разделах | ✅ неверный не пускает, отмена не пускает, верный открывает |
| 13.09.2026 | Цвет экрана и громкость переживают перезапуск | ✅ |
| 13.09.2026 | Свой слог, своё слово, комплект из поставочного и своего | ✅ после добавления этап 1 предлагает 96 слов вместо 95 |
| 13.09.2026 | Поворот сцены к игроку за столом | ✅ покрытие окна 1,00×1,00 на 0° и 90°, ничего не выходит за границы |
| 13.09.2026 | Ничего не выходит за границы окна | ✅ на всех 10 экранах |

---

## 6. Тесты

| Пакет | Тестов | Из них по «АзбукоСлов» |
|---|---:|---:|
| `@kiosk/shared` | 571 | 131 |
| `packages/player` | 690 | 64 |

Разбивка, чтобы цифры можно было пересчитать:

| Модуль | Тестов |
|---|---:|
| `shared` схема модели | 15 |
| `shared` граф | 12 |
| `shared` раскладка ресурсов | 13 |
| `shared` построение вопросов трёх этапов | 16 |
| `shared` ход партии | 19 |
| `shared` статистика | 11 |
| `shared` правила редактора | 33 |
| `shared` свойства виджета | 6 |
| `shared` сверочные векторы | 6 |
| `player` хранилище профилей | 15 |
| `player` хранилище контента | 17 |
| `player` пароль педагога | 7 |
| `player` обмен комплектами | 11 |
| `player` веб-платформа (отладочная) | 14 |

Общий `common/mediaFiles` — ещё 20 тестов, они считаются за Тип 2, где модуль появился.

Домен (граф, три этапа, статистика, правила редактора) закрыт юнит-тестами полностью. Экраны проверяются живым прогоном — это правило сценария, а не недостаток покрытия.
