# 🎉 Версия 1.1.0 - Основные улучшения

## ✨ Что нового

### 1. Цветовая дифференциация виджетов на холсте

Каждый тип виджета теперь имеет свой уникальный цвет:

| Тип виджета | Цвет | Hex |
|-------------|------|-----|
| 🔲 Прямоугольник | Синий | `#4a90e2` |
| 🔘 Кнопка | Зелёный | `#2ecc71` |
| 📝 Текст | Красный | `#e74c3c` |
| 🖼️ Изображение | Фиолетовый | `#9b59b6` |
| 🎬 Видео | Оранжевый | `#f39c12` |

Это делает работу с холстом более наглядной!

### 2. Полноценная привязка к сетке

**Что работает:**
- ✅ Привязка при добавлении виджета на холст
- ✅ Привязка при перемещении виджета
- ✅ Привязка при изменении размера
- ✅ Привязка координат (X, Y)
- ✅ Привязка размеров (Ширина, Высота)

**Как использовать:**
1. Включите сетку (кнопка с иконкой сетки)
2. Активируйте чекбокс "Привязка к сетке"
3. Все виджеты автоматически привязываются к узлам сетки

### 3. Настройки сетки

Новая кнопка ⚙️ рядом с сеткой открывает панель настроек:

**Параметры:**
- **Шаг сетки** (5-100 px) - расстояние между линиями
- **Толщина линий** (1-5 px) - толщина линий сетки
- **Цвет линий** - любой цвет через палитру или HEX

**Быстрые пресеты:**
- **Мелкая** - шаг 10px, линии 1px
- **Средняя** - шаг 20px, линии 1px (по умолчанию)
- **Крупная** - шаг 50px, линии 2px

### 4. Клик по пустому месту снимает выделение

Теперь при клике на пустую область холста все виджеты снимаются с выделения. 
Раньше эта функция уже была, но теперь работает корректнее.

### 5. Улучшенный виджет "Изображение"

#### 5.1 Загрузка изображений

**Два способа:**
1. **По URL** - вставьте ссылку на изображение
2. **Локальный файл** - кнопка "Загрузить файл" (поддерживаются: JPG, PNG, GIF, WebP)

#### 5.2 Отображение на холсте

- Изображение теперь реально отображается в виджете
- Поддержка всех форматов изображений
- Автоматическое масштабирование

#### 5.3 Типы заполнения

В свойствах виджета "Изображение" доступны режимы:

| Режим | Описание |
|-------|----------|
| **Пропорционально (вписать)** | Вписывает всё изображение с сохранением пропорций |
| **Заполнить весь виджет** | Заполняет виджет, обрезая лишнее |
| **Растянуть** | Растягивает на весь виджет, игнорируя пропорции |
| **Оригинальный размер** | Показывает в исходном размере |

#### 5.4 Настройки рамки

**Параметры рамки:**
- ☑️ **Включить рамку** - чекбокс для активации
- **Тип рамки:**
  - Сплошная
  - Штриховая
  - Пунктирная
  - Двойная
- **Толщина рамки** (1-20 px)
- **Цвет рамки** - любой цвет

## 🎯 Как использовать новые возможности

### Настройка сетки

1. Нажмите кнопку с иконкой сетки (включить/выключить)
2. Нажмите кнопку ⚙️ рядом с ней
3. Настройте параметры или выберите пресет
4. Закройте панель кнопкой ✕

### Работа с изображениями

1. Добавьте виджет "Изображение" на холст
2. Выберите его
3. В правой панели:
   - Вставьте URL **или** загрузите файл
   - Выберите тип заполнения
   - При необходимости включите рамку и настройте её

### Привязка к сетке

1. Включите чекбокс "Привязка к сетке"
2. Перемещайте виджеты - они прилипают к узлам сетки
3. Изменяйте размер - размеры кратны шагу сетки
4. Отключите чекбокс для свободного позиционирования

## 📋 Технические детали

### Новые компоненты

- `GridSettings.tsx` - панель настроек сетки
- `ImageWidget.tsx` - компонент для отображения изображений на холсте

### Обновлённые файлы

- `Canvas.tsx` - привязка к сетке, цвета виджетов, ImageWidget
- `PropertiesPanel.tsx` - свойства изображения
- `editorStore.ts` - настройки сетки
- `types/index.ts` - типы для настроек сетки

### Новые свойства в store

```typescript
gridSize: number;        // Шаг сетки (по умолчанию 20)
gridLineWidth: number;   // Толщина линий (по умолчанию 1)
gridColor: string;       // Цвет линий (по умолчанию '#333333')
```

### Новые свойства виджета Image

```typescript
{
  src: string;                    // URL или base64
  isLocalFile: boolean;           // Локальный файл?
  objectFit: 'contain' | 'cover' | 'fill' | 'none';
  borderEnabled: boolean;
  borderStyle: 'solid' | 'dashed' | 'dotted' | 'double';
  borderWidth: number;
  borderColor: string;
}
```

## 🐛 Исправленные проблемы

- ✅ Привязка к сетке теперь работает при всех операциях
- ✅ Клик по фону холста правильно снимает выделение
- ✅ Изображения корректно загружаются и отображаются

## 🚀 Что дальше (в планах)

- Текстовые виджеты с реальным отображением текста
- Виджет кнопки с текстом и стилями
- Видео-виджет с превью
- Группировка виджетов
- Слои и порядок отображения
- Копирование стилей между виджетами

---

**Версия:** 1.1.0  
**Дата:** Декабрь 2025  
**Статус:** ✅ Стабильная версия
