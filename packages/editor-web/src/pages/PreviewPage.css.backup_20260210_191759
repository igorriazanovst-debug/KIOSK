/* PreviewPage.css */

.preview-page {
  width: 100vw;
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: #1a1a1a;
  color: #fff;
  overflow: hidden;
}

/* ========== HEADER ========== */

.preview-header {
  height: 60px;
  background: linear-gradient(180deg, #2a2a2a 0%, #252525 100%);
  border-bottom: 1px solid #3e3e42;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 0 20px;
  flex-shrink: 0;
}

.preview-title {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.preview-title span:first-child {
  font-size: 16px;
  font-weight: 600;
  color: #fff;
}

.preview-resolution {
  font-size: 12px;
  color: #888;
  font-family: 'Courier New', monospace;
}

.preview-close-btn {
  display: flex;
  align-items: flex-start;
  justify-content: center;
  width: 40px;
  height: 40px;
  background: #333;
  border: 1px solid #444;
  border-radius: 6px;
  color: #ccc;
  cursor: pointer;
  transition: all 0.2s;
}

.preview-close-btn:hover {
  background: #3a3a3a;
  border-color: #555;
  color: #fff;
}

/* ========== CANVAS CONTAINER ========== */

.preview-canvas-container {
  flex: 1;
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding: 20px;
  overflow: auto;
  background: #0f0f0f;
}

.preview-canvas {
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.5);
  position: relative;
}

/* ========== FOOTER ========== */

.preview-footer {
  height: 40px;
  background: #252525;
  border-top: 1px solid #3e3e42;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 0 20px;
  font-size: 12px;
  color: #888;
  flex-shrink: 0;
}

/* ========== LOADING STATE ========== */

.preview-page.loading {
  align-items: flex-start;
  justify-content: center;
  gap: 20px;
}

.loading-spinner {
  width: 50px;
  height: 50px;
  border: 4px solid #333;
  border-top-color: #4a90e2;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

.preview-page.loading p {
  font-size: 16px;
  color: #888;
}

/* ========== ERROR STATE ========== */

.preview-page.error {
  align-items: flex-start;
  justify-content: center;
  gap: 20px;
}

.preview-page.error h2 {
  font-size: 24px;
  color: #f44336;
  margin: 0;
}

.preview-page.error p {
  font-size: 14px;
  color: #888;
  margin: 0;
}

.preview-page.error button {
  padding: 10px 20px;
  background: #4a90e2;
  border: none;
  border-radius: 6px;
  color: #fff;
  font-size: 14px;
  cursor: pointer;
  transition: background 0.2s;
}

.preview-page.error button:hover {
  background: #357abd;
}

/* ========== POPUP ========== */

.preview-popup-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  display: flex;
  align-items: flex-start;
  justify-content: center;
  z-index: 10000;
  animation: fadeIn 0.2s;
}

.preview-popup {
  background: #2a2a2a;
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
  display: flex;
  flex-direction: column;
  max-width: 90vw;
  max-height: 90vh;
}

.preview-popup-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 16px 20px;
  background: #333;
  border-bottom: 1px solid #444;
}

.preview-popup-header h3 {
  margin: 0;
  font-size: 18px;
  color: #fff;
}

.preview-popup-header button {
  display: flex;
  align-items: flex-start;
  justify-content: center;
  width: 32px;
  height: 32px;
  background: #444;
  border: none;
  border-radius: 4px;
  color: #ccc;
  cursor: pointer;
  transition: all 0.2s;
}

.preview-popup-header button:hover {
  background: #555;
  color: #fff;
}

.preview-popup-content {
  padding: 20px;
  color: #ccc;
  font-size: 14px;
  line-height: 1.6;
  overflow: auto;
  flex: 1;
  white-space: pre-wrap;
}

/* ========== ANIMATIONS ========== */

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

/* ========== RESPONSIVE ========== */

@media (max-width: 768px) {
  .preview-header {
    height: 50px;
    padding: 0 12px;
  }

  .preview-title span:first-child {
    font-size: 14px;
  }

  .preview-resolution {
    font-size: 11px;
  }

  .preview-canvas-container {
    padding: 12px;
  }

  .preview-footer {
    height: 36px;
    font-size: 11px;
  }

  .preview-popup {
    max-width: 95vw;
    max-height: 95vh;
  }
}
